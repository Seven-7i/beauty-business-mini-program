# 小程序 UI 图说明

> 更新时间：2026-09-16
> 图稿只表达视觉方向；字段、状态和行为以 `docs/微信小程序需求文档.md` 与《交互说明.md》为准。

> 上线前工程基线：美容业务的 16 个路由及业务组件归入 `pages-beauty` 独立分包，主包只保留首页、备份恢复和模块管理。美容模块样式变量由 `pages-beauty/styles/_tokens.scss` 私有维护，其他后续模块建立各自的 token 文件，不在应用启动时全局引入业务模块样式。微信端为兼容已验证的六位模块码组件保留非压缩构建，并通过 `pnpm check:mp-weixin-size` 持续检查主包、各分包 2 MiB 和总包 20 MiB 上限。

> 2026-09-17 09:30（UTC+8）真机反馈修正版：撤回全局按钮机械放大，恢复预约、顾客、项目、库存和历史清理页面原定比例；“完成预约”恢复为 `166rpx × 70rpx` 紧凑按钮。六位模块码恢复为已验证的 `up-code-input` 六格输入，微信构建继续使用 `minified: false` 规避既有真机兼容问题。lint、74 个测试文件共 405 项测试、微信与 H5 正式构建、包体积检查和 `git diff --check` 均通过；本地构建主包 `901.1 KiB`、美容分包 `608.2 KiB`、总计 `1509.3 KiB`。build 模拟器已核对预约执行与模块管理页面，控制台 error 过滤为空；09:30 重建的 `app.js` SHA-256 为 `599C556F0DF3BD830807042AB807912BE735B227464CC405029CDECD80189B2A`。用户明确选择推送后，以 `pages/index/index` 为入口执行 `auto_preview` 成功；微信工具返回总包 `2079370` bytes、主包 `1279323` bytes、美容分包 `800047` bytes。当前状态仅为“修正版预览包推送命令成功，待用户手机确认”，不表示真机已更新、体验版已上传或正式发布；上一版 21:18 预览不再作为验收依据。

> 全局交互基线：所有可执行按钮和语义操作区均提供即时按压反馈。原生按钮统一采用轻微压暗与 `0.98` 缩放，非按钮卡片、列表行、Tab 和文字入口通过共享按压态获得相同反馈；禁用态保持静止，已有页面专属按压色继续优先使用。

> 2026-09-14 全局点击反馈实现已完成：新增源码契约自动扫描所有 Vue 组件中的 `@click` / `@tap` 操作区，防止后续页面再次遗漏按压态；预约日历日期、预约列表卡片、项目选择操作区和美容数据入口已补齐共享反馈。Vue 类型检查、73 个测试文件共 399 条测试、微信发布构建及 `git diff --check` 均通过；发布 `app.wxss` SHA-256 为 `51A0886573393C4DD6AA9C5F49CC97B825FA56D794341377C6210934FF008BCA`，更新时间为 2026-09-14 23:08:43（UTC+8），已核对包含 `button:active`、`button[disabled]:active` 与 `.app-pressable`。用户于 2026-09-15 选择推送后，以 `pages/beauty/index` 为入口执行 `auto_preview` 成功，包体为 `2061651` 字节；当前仅表示推送命令成功，待用户手机确认后才能标记“真机已更新”。

所有路由页的微信原生标题栏背景跟随该页所属画布底色，避免标题区域与页面主体出现白色、灰色或异色断层；全局页面继续使用各自的平台底色，美容业务页面继续使用各自已确认的浅粉或紫粉底色，不跨页面统一成单一主题色。

## 当前全局页面图稿

工作台定稿为 `13-工作台-模块门厅-无管理入口-v5.png`；“我的”定稿为 `12-我的-本机控制中心-v1.png`。两者共同约束全局应用层页面，不向美容等具体业务模块扩散。

- 工作台定位为独立的“模块门厅”，只负责展示和进入业务模块，不继承任何具体业务模块的风格。
- 使用暖石色底、墨色文字、靛灰平台色和低饱和玫瑰色模块标识。玫瑰色只用于美容模块局部，不作为工作台全局主题色。
- 通过半透明面板、背景模糊、柔和环境色块、细描边和分层阴影建立 Apple 式空间层次；禁止用照片或纹理图模拟高斯模糊。
- 工作台不展示美容模块的“待处理”数据；模块业务数据进入对应模块后查看。
- 工作台不展示数据保护入口；数据导出、恢复和容量管理统一放在“我的”。
- 工作台不再提供“模块管理”入口；模块管理统一放在“我的”的“数据与管理”分组中。
- 底部工作台图标使用统一靛灰线条小房子，选中态由磨砂底和颜色表达，不使用黑色实心块。
- 单模块启动直达已于 2026-09-15 实现：只有一个已解锁模块时直接进入该模块首页，全局工作台仍作为返回层承载模块入口与“我的”；本轮不改变既有图稿视觉。Vue 类型检查、74 个测试文件共 402 条测试、微信发布构建及 `git diff --check` 均通过；构建于 19:49:28（UTC+8）开始，`pages/index/index.js` 于 19:49:41 更新，SHA-256 为 `29AF60A94A083BACFA0735F9BC68613371691D62ED8BF4444C70D1AD8797007B`。build 模拟器从 `pages/index/index` 启动后的实际页面栈为 `pages/index/index → pages/beauty/index`，控制台错误过滤为空。用户授权后于 19:55:41（UTC+8）以 `pages/index/index` 为入口执行 `auto_preview` 成功，包体为 `2062748` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机实际进入美容模块后才能标记“真机已更新”。

### “我的”当前方向

- “我的”与工作台使用同一套暖石色背景、半透明磨砂面板、环境色块、细描边和分层阴影，不另设全局主题。
- 页面定位为本机控制中心，不展示账号资料、头像或社交身份；首要信息是本机存储状态。
- 数据备份与恢复、模块管理和使用说明集中在“数据与管理”分组中，使用统一线条图标和轻量右箭头。
- 底部导航结构与工作台保持一致，仅切换“我的”的选中态。
- `12-我的-本机控制中心-v1.png` 已确认并进入代码实现。

### “数据备份与恢复”当前方向

- `14-数据备份与恢复-本机数据保护-v1.png` 是从“我的”进入的系统级子页面定稿，延续同一套暖石色磨砂空间风格，不设置底部全局导航，已进入代码实现。
- 首屏先展示“最近完整系统导出”，让用户立即判断是否需要备份；导出区明确区分“完整系统备份”和“选择模块导出”。
- 两个导出卡片行本身是入口，行尾统一使用箭头；点击后先确认范围与未加密风险，再生成文件，不使用选择后动态出现的“立即导出”按钮。
- 微信转发返回后使用强确认弹窗询问实际发送结果；若未处理便退出，下次进入程序再次弹出“上次导出尚未确认”，不会仅依赖页面内弱提示。
- “已发送”一经本机保存便不可改回“未发送”；后续异常只提供完成记录的单向重试，避免最近导出状态与用户决定相互矛盾。
- 冷启动确认采用单次门禁：同一条待确认只打开一个弹窗，处理后本次启动不再紧接容量或七天提醒。
- 恢复入口单独成卡，先选择并校验文件，再进入范围摘要与覆盖确认；页面不使用“合并”“云端同步”或“加密备份”等错误暗示。
- 未加密文件风险使用低干扰提示卡常驻说明；真正导出或恢复前仍按交互规则进行明确确认。

### “模块管理”当前方向

- `15-模块管理-本机授权-v1.png` 是已确认定稿，延续“我的”和系统级数据保护页的暖石色磨砂空间风格，不设置底部全局导航，已进入代码实现。
- 页面先展示当前已解锁模块，再提供六位模块授权码输入和“开启模块”操作；单模块场景不展示无效的默认模块设置。
- 美容模块沿用低饱和玫瑰色身份标识，授权与操作控件继续使用靛灰平台色，不把美容模块内部风格扩散到系统页面。
- 第一版只允许增加已解锁模块，不提供移除、删除或停用入口。
- 真机预览状态：已推送，待用户真机反馈。

## 当前美容模块页面图稿

`美容模块/16-美容管理-模块首页-v1.png` 是美容模块首页已确认定稿，已进入代码实现。

- 页面归属美容模块工作台，使用浅粉背景、雾紫强调色和鼠尾草绿状态色，不继承全局工作台/“我的”的平台主色。
- 首屏按“经营概览 → 近期预约 → 业务入口”组织，经营概览只展示本月完成、本月成交和待执行三项既有指标。
- 近期预约以高不透明度实体行展示已逾期、今天和未来三天的待执行预约，不使用顾客头像、项目照片或图片占位；预约行提供浅粉压暗、描边、内阴影与轻微缩放的按压态，点击后进入该预约详情，“查看全部”独立进入预约执行列表。
- 业务入口只包含预约执行、顾客管理、服务项目和物品库存；四项使用独立实体按钮卡片、右箭头和按压反馈明确可点击性；底部导航保持美容模块内部的首页、日程、报表和数据四个页签。
- 毛玻璃只用于经营概览和底部选中态；高频列表与入口使用实体面板，图标均按可由 `AppIcon` 复现的语义线条设计。
- 模块内页签切换时同步原生标题栏背景：美容首页跟随顶部 `#FFF8FA` 浅粉，日程和数据跟随 `#FBF5F7` 浅粉，报表跟随顶部 `#FFF2F6` 紫粉；标题区和当前内容区不再出现白色、灰色或异色断层。
- 2026-09-14 21:20（UTC+8）：完成全部 19 个路由的标题栏与页面画布底色核对，并修正美容模块四页签动态标题栏及顾客列表、新增/编辑顾客、顾客详情三条独立路由。类型检查、72 个测试文件共 396 项、发布构建和 `git diff --check` 均通过；发布产物 `pages/beauty/index.js` SHA-256 为 `F9E7EDEC20CBF625552722E86CC45B795D8A19501FFE79F7ECF46F19DF670F78`，确认包含四页签颜色映射及 `setNavigationBarColor` 调用。随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2057685` 字节；当前仅表示本轮预览包推送命令成功，待用户真机确认。
- 2026-09-14 21:45（UTC+8）：补齐近期预约实体行的微信原生 `hover-class` 按压反馈，发布产物 WXML 已包含零延迟触发与 `100ms` 保持时间，WXSS 已包含浅粉压暗、描边、内阴影和 `0.985` 缩放。类型检查、72 个测试文件共 396 项、发布构建和 `git diff --check` 均通过；`BeautyAppointmentReminders.wxml` 与 WXSS 的 SHA-256 分别为 `D728786B151B621058D3D9E0C017DCBB4351DAD07E8EA1CB426241D0532C64E5`、`6B31BDA7D61D2AE144021121D74A36A9F515F487E784BE262A7F9F380452FF25`。随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2058315` 字节；当前仅表示本轮预览包推送命令成功，待用户真机确认。
- 2026-09-14 21:55（UTC+8）：纠正近期预约的跳转语义，单条预约行携带预约标识进入独立详情，“查看全部”继续进入预约执行列表。类型检查、72 个测试文件共 397 项、发布构建和 `git diff --check` 均通过；`pages/beauty/index.js` 与 `BeautyAppointmentReminders.js` 的 SHA-256 分别为 `369C0AE0165B300F7C6DF5DFDC04F9C8A75495B5E79E2378A84F130160AA5ED7`、`C9125E19DD04A38AB9186749F728DEA1878A1D311BCEECAC4C1B975460C679C1`，构建产物确认详情路径与列表事件相互独立。随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2058315` 字节；当前仅表示本轮预览包推送命令成功，待用户真机确认。
- 用户反馈上述自动推送未到达手机后，本轮状态改记为“真机未更新”。2026-09-14 22:01（UTC+8）重新执行发布构建，关闭同 AppID 的 dev/build 两个旧项目窗口，仅重开 build 项目并清理 `cleanCompileCache`，重新进入 `pages/beauty/index`；随后生成新窗口预览二维码并再次执行 `auto_preview`，两次工具返回的包体一致为 `2058855` 字节。当前仍须用户扫码或确认手机收到后，才能标记“真机已更新”。
- 真机预览状态：上一轮自动推送未到达；已基于清缓存后的 build 产物生成新二维码并重新推送，待用户扫码或确认手机收到。

### “数据”当前方向

- `美容模块/37-美容数据-数据保护-v1.png` 已确认定稿，归属美容模块内部“数据”页签，不扩散到系统级“我的”或“数据备份与恢复”页面。
- 延续美容模块的浅粉背景、雾紫强调色、柔和光晕和高不透明度实体卡；首屏以“最近导出”提示七天保护节奏，随后提供“导出美容模块”和“从备份恢复”两项独立入口，底部导航仅选中“数据”。
- 恢复入口只表达“先校验、再明确确认覆盖”的既有流程；风险提示明确备份文件未加密并含顾客资料，不增加云端同步、合并恢复或加密备份等错误暗示。
- 当前状态：已完成实现与验证。`BeautyDataPanel` 将导出和恢复入口收敛为模块内实体操作卡，展开后复用既有文件生成、微信转发确认、只读校验和覆盖确认状态流；导出状态卡仅显示美容模块最近导出记录。2026-09-13 00:21:33（UTC+8）发布构建产物 `BeautyDataPanel.wxml` 已确认包含“数据安心放在手边”“导出美容模块”和“备份文件未加密”，SHA-256 为 `27F040568EFE7DC3D657AAF4EB104A0522963018EAE3C6337169ECC15C0F8031`。类型检查、71 个测试文件共 390 项、发布构建和 `git diff --check` 均通过；随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2021261` 字节。当前仅表示本轮预览包推送命令成功，待用户确认手机显示本轮数据页后才能标记“真机已更新”。
- `美容模块/39-美容数据-操作展开态-v1.png` 的展开态已确认：导出和恢复任一入口展开时，入口卡片本身切换为雾紫实底、白色文字与图标、向上箭头；展开内容紧贴其下使用浅紫面板，未选入口维持白色实体卡。同一入口再次点击时收起，点击另一入口时直接切换，入口卡的底部圆角与详情面板的顶部边框在展开态合并为一个连续折叠层级。页面不显示“庄月空间 · 美容”或“数据安心放在手边”页内标题，首屏直接从“最近导出”卡片开始。2026-09-13 22:28:21（UTC+8）构建产物 `BeautyDataPanel.wxml` SHA-256 为 `6D4FCE8E48A0AD1B58AEB34DD4F8F252B3CB5C43742F748CDC3D1C4663F76EBA`，确认不含已移除的两段标题且保留导出、恢复入口；类型检查、72 个测试文件共 393 项测试、发布构建和 `git diff --check` 均通过。随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055694` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机显示本轮变化后才能标记“真机已更新”。
- 2026-09-14 20:32:42（UTC+8）：补齐操作卡折叠契约。同一入口再次点击会收起，右箭头随展开态旋转为向上，入口卡与详情面板使用连续圆角和边框层级；模拟器运行时已依次断言“导出展开 → 导出收起 → 恢复展开”。类型检查、72 个测试文件共 394 项、发布构建和 `git diff --check` 均通过；`BeautyDataPanel.js`、WXML、WXSS 的 SHA-256 分别为 `2FC8DC609C468C3DB778091677EBEAC12DAAD6A312FD2119078805121497321F`、`2DFF9C6D7C81E9F2D7B0E5AE27692A764F56B582FD5740F9FC7C0564090038E3`、`1D9CA4EAEE7B975181A1CAFC380C61313F5488C52D71507B2CF25B1732F0E4BF`。随后以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2056936` 字节；当前仅表示本轮预览包推送命令成功，待用户真机确认。
- 用户反馈真机未收到后，确认同一 AppID 同时导入 dev 与 build 两个项目；已对 `app/dist/build/mp-weixin` 清理 `cleanCompileCache`，重新编译并打开 `pages/beauty/index`，再执行 `auto_preview` 成功，包体为 `2055857` 字节。当前仍为“真机未更新/待用户确认”，不以工具成功替代手机端结果。

### “日程”当前方向

- `美容模块/36-预约月历-日程-v2.png` 已于 2026-09-09 确认定稿并完成代码实现，页面归属美容模块内部“日程”页签，延续美容模块浅粉背景、雾紫强调色、鼠尾草绿状态色和高不透明度实体预约卡。定稿不再展示页内顶部身份文字或页内大标题，主体从月历卡片开始；月视图标记存在预约的日期，选中日期后在下方展示当天预约列表；底部模块导航选中“日程”，并提供“管理预约”进入预约执行列表。
- 第一版预约月历不加入拖拽改期、时间轴排班或微信消息提醒。月历区向上滑收起为当前选中日期所在的一周，向下滑展开为整月；左右滑动按当前形态切换上/下月或上/下周，周视图允许跨月。当前选中日期不是今天时，右上角显示雾紫轻量“回到今天”操作；选中今天时不显示该操作，工具栏仍保持固定高度。切换月份时默认选中新月份 1 日。
- 2026-09-09：日程页按 v2 定稿完成实现，移除页内顶部身份文字和页内大标题，补齐“今天”按钮回到真实今天的状态流，并增加 UI 契约与 composable 单元测试。`npm run type-check`、70 个测试文件共 390 条测试、`npm run build:mp-weixin` 和 `git diff --check` 均已通过；发布产物 `AppointmentCalendar.js`、`AppointmentCalendar.wxml`、`AppointmentCalendar.wxss` 与 `pages/beauty/index.js` 于 23:12（UTC+8）更新，SHA-256 分别为 `4E9C9ECC707B964D4159F88EA0ACD5CE1A7F12ED63F4B72F01D92589F51CD871`、`317FB6FFEA3B40DD25FCE864E5750B773BBC4510FC742D611927855AF683C42E`、`EB2CDC8EED4BB0B83712B1E2D4CABE21FE1670586E2167898C93BEF647F5E8A2` 和 `3F0EA038E3FBE0F50D3768F843A24C2EED90DE44B46C81EDB2FADA2FB53826F1`。磁盘产物确认不再包含 `预约视图`、`calendar-panel__heading`、`calendar-panel__eyebrow` 或页内 `calendar-panel__title`，并包含 `go-today` 事件和 `calendar-card__month-title`。build 模拟器已打开 `pages/beauty/index` 且控制台 error 过滤为空；自动化层未能可靠点击模块内“日程”页签。

- 2026-09-12：用户明确授权后重新完成类型检查、70 个测试文件共 390 条测试、发布构建和 `git diff --check`。构建产物于 15:56:27（UTC+8）更新，`AppointmentCalendar.js`、`.wxml`、`.wxss` 的 SHA-256 分别为 `4E9C9ECC707B964D4159F88EA0ACD5CE1A7F12ED63F4B72F01D92589F51CD871`、`317FB6FFEA3B40DD25FCE864E5750B773BBC4510FC742D611927855AF683C42E`、`EB2CDC8EED4BB0B83712B1E2D4CABE21FE1670586E2167898C93BEF647F5E8A2`。build 模拟器已打开 `pages/beauty/index`、美容模块首页截图正常且 console 的 error 过滤为空；以 `pages/beauty/index` 执行 `auto_preview` 成功，完成时间为 15:58:58（UTC+8），包体为 `2013164` 字节。此状态仅表示预览包推送命令成功，待用户确认手机实际显示本轮“日程”页后才能标记“真机已更新”。

- 2026-09-12：缺失的 `美容模块/36-预约月历-日程-v2.png` 已按当前日程页视觉重新生成并经用户确认，包含雾紫“回到今天”、无左右切换按钮的月历、预约列表及日程选中态底部导航；当前作为已确认重建稿。

### “报表”当前方向

- `美容模块/38-轻量经营统计-报表-v2.png` 已确认定稿并完成代码实现，归属美容模块内部“报表”页签，延续浅粉背景、雾紫强调色与半透明白色卡片。
- 页面不设置页内大标题，展示本月完成数、成交金额、客单价和按服务项目归集的贡献；不展示趋势图。
- 该页仅用于轻量经营复盘，不加入利润、支出、记账、复杂筛选或密集表格；底部模块导航选中“报表”。
- 月份选择器已补充真实切换交互：左侧查看历史月份，右侧逐月返回且不超过当前月；切换后四项汇总和服务贡献同步重算。
- 2026-09-13：根据真机截图完成视觉回归重构。月份胶囊、四列汇总卡、字号层级、排行编号、圆形语义图标、贡献比例条、卡片圆角与空状态均按 v2 定稿建立源码契约；不再使用连续覆盖样式微调。
- 2026-09-13 19:38:51（UTC+8）：类型检查、72 个测试文件共 392 项、发布构建和 `git diff --check` 均通过。关闭并重开复用旧依赖缓存的 build 项目窗口后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2050476` 字节；`BeautyReportSummary.wxml` 与 `BeautyServiceContribution.wxml` 的 SHA-256 分别为 `1AF0DF708FF83A6CE97463CB0C64B4B0CD05C2B7D13C80F0E627C71DA49E10B9`、`EBA489C9D8F960981F647DC8DF26CB00ECDFB5AA8A1A5344F67AA436142437CF`。当前仅表示本轮预览包推送命令成功，待用户确认手机显示本轮报表视觉后才能标记“真机已更新”。
- 2026-09-13 19:54:37（UTC+8）：修复月份箭头仅展示但不可操作的问题。历史月份切换、当前月上限和汇总同步重算已加入测试；类型检查、72 个测试文件共 393 项、发布构建及 `git diff --check` 均通过。首次预览仍返回旧包体 `2050476` 字节，因此未作为有效推送；重开 build 项目窗口并清理 `cleanCompileCache` 后，以 `pages/beauty/index` 再次执行 `auto_preview` 成功，新包体为 `2056102` 字节。`BeautyReportSummary.wxml` 与 JS 的 SHA-256 分别为 `314658F5FB07E59AC32400E0C48864A1C2DE3DF240F502ECB1D62C6A1CE64DA8`、`EF1AF81B3FBDC1E9B76DD393B17D4F11094D172A4263B04B925423CC266B6AC1`；当前仅表示月份切换修正版预览包推送命令成功，待用户真机确认。
- 真机截图复核后继续校正视觉：月份文字固定单行居中，左右点击区改为绝对定位以免挤压年月文字；箭头恢复定稿深色。无服务数据时移除额外图标和二级标题，恢复为定稿中的单行说明与紧凑卡片高度。
- 真机反馈底色偏白后，将月份胶囊与四项汇总卡改为固定雾紫渐变，不再使用会被页面白色冲淡的半透明叠色；页面底色同步补强紫粉光晕层次。
- 2026-09-13 20:30（UTC+8）：雾紫底色修正版通过类型检查、72 个测试文件共 393 项、发布构建和 `git diff --check`；重开 build 项目并清理 `cleanCompileCache` 后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055426` 字节。`BeautyReportSummary.wxss` 与 `BeautyReports.wxss` 的 SHA-256 分别为 `A07CF9A78BB7FF791E2320E98174FE8445A9D4B488631E8E844CFD0601A6F0ED`、`92805579E943FE9D5C768F01D701351D2A35BE9EF4049400A5FE4355018B7841`；当前仅表示预览包推送命令成功，待用户真机确认底色。
- “较上月”颜色按用户确认采用国内股票习惯：正增长红色、负增长绿色，持平或缺少上月基数时为灰色。
- 月份左右箭头使用相同的定位盒和垂直居中规则；左箭头只水平镜像图形，不再旋转整个点击区域，避免图标字体基线在真机上一高一低。
- 月份选择器最终改为固定三列网格，左箭头、年月文字和右箭头共用同一垂直中心线；年月列保留独立宽度并强制单行，避免只对齐箭头却与文字错位。
- 真机截图确认字体图标即使容器对齐仍存在字形留白偏差；月份箭头改为同尺寸、同边框的 CSS 几何图形，只以旋转方向区分左右，从源头消除字体基线和字形留白造成的视觉错位。
- 2026-09-13：几何箭头修正版通过类型检查、72 个测试文件共 393 项、发布构建及 `git diff --check`；重开 build 项目并清理编译缓存后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055694` 字节。`BeautyReportSummary.wxml` 与 WXSS 的 SHA-256 分别为 `2EF3C45F900F30606F45B78749EF3890DC4F90A53F6B70E5B6E740B13187A874`、`1A536CCD8EBBB33176B19129EAEB5868EC362CD2C638F7BDB25CC8A0E19164E4`；当前仅表示预览包推送命令成功，待用户真机确认。
- 2026-09-13：三列共同居中修正版通过类型检查、72 个测试文件共 393 项、发布构建及 `git diff --check`；重开 build 项目并清理编译缓存后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055959` 字节。`BeautyReportSummary.wxml` 与 WXSS 的 SHA-256 分别为 `86D2FC99CADBD93291D4D2B72C0BE3E4AFD7D95A5DA4F40F62D222F0BDA66ED9`、`7E0692292E0142321595B684157DDF89E8C1196494F3C719F2DF62ECD7E3C6D8`；当前仅表示预览包推送命令成功，待用户真机确认。
- 2026-09-13：箭头水平对齐修正版通过类型检查、72 个测试文件共 393 项、发布构建和 `git diff --check`；重开 build 项目并清理编译缓存后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2056026` 字节。`BeautyReportSummary.wxml` 与 WXSS 的 SHA-256 分别为 `D0E58ECD268C4CB5449E1D3BA44E662942FE796FD973B9140D3028537F7CECAA`、`C21E5FBA82B4D0BAE2A39C25ED5FADD7220E994B2A524CF8C4941A18F4DA5E65`；当前仅表示预览包推送命令成功，待用户真机确认。
- 2026-09-13：涨红跌绿修正版通过类型检查、72 个测试文件共 393 项、发布构建及 `git diff --check`；重开 build 项目并清理编译缓存后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055930` 字节。`BeautyReportSummary.js` 与 WXSS 的 SHA-256 分别为 `0304E2E0C08A14A6DFEFD9E627264D4D60A8619BED290F05A580C2DBA56F4760`、`1152EA792602CCB03BD1742779769250F631F2BECFF44563951354DD340577FA`；当前仅表示预览包推送命令成功，待用户真机确认。
- 2026-09-13 20:21:01（UTC+8）：上述视觉修正版通过类型检查、72 个测试文件共 393 项、发布构建和 `git diff --check`。重开 build 项目窗口并清理 `cleanCompileCache` 后，以 `pages/beauty/index` 执行 `auto_preview` 成功，包体为 `2055368` 字节；`BeautyReportSummary.wxml` 与 `BeautyServiceContribution.wxml` 的 SHA-256 分别为 `3E950D68C0238F5ECA29065BA1CC7EE60191B427D477D3CBEF0340D6CBF78D0F`、`C9465F4C1B9FC4D58BEC4E6988DFF539B2E82FFEB6675D1324071DF0A922C0EE`。当前仅表示视觉修正版预览包推送命令成功，待用户真机确认。

### “预约执行”当前方向

- `美容模块/30-预约执行-待执行列表-v4.png` 已于 2026-09-05 确认定稿并完成代码实现，页面归属美容模块业务子页面，延续浅粉背景、雾紫强调色、鼠尾草绿状态色和高不透明度实体预约卡，不设置模块底部导航；v1 至 v3 已淘汰移除。
- 列表顶部只保留统一“新增”入口，不设置独立“补预约”或“月历”按钮；后补预约绝不进入待执行范围，因此当前待执行列表不显示后补标签。后补标识只随已完成或已取消记录展示，不参与预约状态表达；预约月历仅从美容模块“日程”页进入。
- `美容模块/31-新增预约-正常模式-v3.png` 已确认定稿并完成代码实现：统一新增页顶部先选择“正常预约 / 后补预约”，默认选中正常预约；预约类型和后补完成情况使用外层统一边框、选中项浅紫底的无原生按钮内层分段控件，所有选中项都统一显示勾选图标，避免真机出现独立内描边或选中反馈不一致。正常模式只填写顾客、预约项目组合、预约开始时间、预约服务地址和选填备注，并展示自动汇总的预计总时长与标准金额，不展示或编辑本次实际用量，待执行库存占用由项目默认用量自动计算。v1、v2 已淘汰移除。
- 新增预约的“选择项目”改为底部多选面板：服务项目行左侧显示名称、标准金额和时长，右侧使用 uview-plus 复选框；支持搜索、整行勾选、一次确认多个项目，确认后才统一更新项目组合与汇总值。
- 2026-09-06：项目多选面板已实现并接入正常预约、后补完成和后补未完成共用的预约表单。面板以独立临时选择状态承载搜索和勾选，取消不会修改表单；确认后去重、过滤不可用项目，并一次性刷新项目组合、金额、时长及后补完成的默认实际用量。`npm run type-check`、69 个测试文件共 385 条测试与 `npm run build:mp-weixin` 均已通过；开发者工具已清理 build 编译缓存并重新打开 `pages/appointment-create/index`，控制台无错误。构建产物 `AppointmentForm.js`、`AppointmentForm.wxml`、`AppointmentProjectMultiSelect.js`、`AppointmentProjectMultiSelect.wxml` 的 SHA-256 分别为 `22729659C4F070F6A0597BB3050B4EEDB91832B61C1DCD698C1F7493D9A9D0F7`、`A3C6E2E5512BB2F7401C4E33593E9662EFF68FE4C5F65B68FE3497F145B2F710`、`5749FE34E0A1E5CBDF1EBE466374EF67B7CC79244896187A896007B3B4906800`、`AFFF397598B03410DD01399297B221D15BDC01742833919CEA226EF479DC5308`。尚待真机预览确认。
- 用户于 2026-09-06 明确授权后，新增预约入口 `pages/appointment-create/index` 的项目多选修正版私有预览包已成功推送至当前登录的微信开发者账号，包体为 `1941415` 字节；等待真机确认右侧多选勾选与一次确认行为。
- 2026-09-06：真机反馈项目多选面板无法勾选。已移除 uview-plus `u-checkbox-group` 的跨组件事件同步，改为每项 `u-checkbox` 的 `used-alone` 与 `checked` 直接绑定面板临时选择状态；整行与勾选框均调用同一切换逻辑，保留右侧勾选布局。`npm run type-check`、69 个测试文件共 385 条测试和 `npm run build:mp-weixin` 均通过；清理 build 编译缓存后重新打开 `pages/appointment-create/index`，控制台无错误。修复后 `AppointmentProjectMultiSelect.js` 与 WXML 的 SHA-256 分别为 `0EE8BC050C277FC9B4510C056DAC81669C4068C75DFD49048C25F7014D126626`、`74137D097DE50705E230BC2411D6B4718C7E1AFA80A865C1EAF86E19148D4A80`，尚待真机预览确认。
- `美容模块/32-预约详情-正常待执行-v3.png` 已确认定稿并完成代码实现：普通待执行详情不显示后补标签，把项目默认用量称为“预计占用”，明确提示完成时再确认实际用量，不把尚未发生的消耗展示为本次实际用量。“预约信息”按左侧字段名、右侧字段值的双列方式排列，长内容仅在右列换行。v1、v2 已淘汰移除。
- 详情页仍将“完成预约”保持为待执行状态的主操作，将改期、取消预约和彻底删除误建预约收敛到页面底部；后补预约不会出现此待执行详情状态。
- `美容模块/33-新增预约-后补完成-v3.png` 已确认定稿并完成代码实现：与正常预约共用同一新增页面，顶部选中“后补预约”后显示“已完成 / 未完成”，默认选择已完成，绝不提供待执行；已完成时展示本次实际用量、成交金额和实际完成时间，本次实际用量默认带出项目正常用量并允许调整。v1、v2 已淘汰移除。
- `美容模块/34-完成预约-实际用量弹窗-v1.png` 已确认定稿并完成代码实现：普通待执行预约点击“完成预约”后在当前详情页打开底部弹窗，优先带出预约创建时保存的预计用量；历史记录未保存预计用量时回退项目正常用量，并在同一弹窗确认成交金额、实际完成时间和完成备注；不再跳转独立确认页。
- `美容模块/35-新增预约-后补未完成-v1.png` 已确认定稿并完成代码实现：后补模式选择未完成表示顾客取消，隐藏实际用量、成交金额和实际完成时间，要求选择取消原因并可填写补充说明；保存后直接成为带后补标识的已取消预约，不占用或扣减库存。
- 上述六张视觉稿已由用户统一确认。代码新增预约列表、新增预约和预约详情三条路由，完成与取消使用详情/列表内底部弹层；弹层内就地显示库存或保存错误，提交中不会被遮罩误关。后补完成通过单条仓储事务直接落为已完成并按历史时间回放库存，后补未完成直接落为已取消，持久层不会出现后补待执行中间状态；列表与详情对后补取消统一展示“未完成”。预约和美容模块数据契约已升为 v2，并提供 v1→v2 迁移；旧版无取消原因记录会写入明确的历史占位原因，且已覆盖迁移幂等、分片仓储读取和备份预检。正常完成会保留预计用量用于撤销恢复。Vue 类型检查、69 个测试文件共 379 条测试、`git diff --check` 和微信发布构建均已通过；构建产物于 2026-09-05 20:20:43（UTC+8）更新，`AppointmentForm.js`、`AppointmentDetailPage.js` 与 `business-data-mutation.js` 的 SHA-256 分别为 `CA107094C22F54FE98771C13C861B1B4F7EDB8D230AF7D3FD70E978DC81577EF`、`8FB275B274C3EBD501A1C785EF1A3D5DD4D91DFF39B80F65AD2FB7E5D784EE46` 和 `D4DAC3B055F1CD92EC2E385CA0649BD697EC8C1958C0FFF662099C157904C0DB`。build 模拟器确认当前页为 `pages/appointment/index`、原生标题“预约执行”，关键表单与弹层 WXML/WXSS 编译通过且控制台无错误。用户于 2026-09-05 22:37:21（UTC+8）明确授权后，首版私有预览包已自动上传到当前微信开发者账号（AppID `wx4275c797d569957a`），包体为 `1906680` 字节。用户随后反馈新增预约资料不足时说明与按钮不对应；修正版改为按缺失项动态展示说明和入口，两项都缺失时同时显示“新增顾客”和“新增服务项目”，只缺一项时仅显示对应说明与按钮。修正版 `AppointmentCreatePage.js` 与 WXML 于 22:51:36 更新，SHA-256 分别为 `224C90A8FCE474B0EE33A509CAF968A69F5FA7EC270F6493BD9664854ACEAF76` 和 `3AC99F3D06D4CC8A8DB3DD4F8C0A5C21E7FEF5BF7EA7D163806AF0AFE1929E4C`；模拟器已确认“只缺服务项目”时说明和“新增服务项目”按钮一致，模板与样式编译通过且控制台无错误。22:55:45 以 `pages/appointment-create/index` 为入口重新执行 `auto_preview` 成功，修正版包体为 `1908358` 字节。用户随后反馈“预约类型”和“完成情况”选中项存在原生按钮内层样式；二次修正版将两组控件改为 `view role="tab"`，由外层统一承担边框与圆角，选中项只保留浅紫底、紫色文字和勾选图标。`AppointmentForm.js`、WXML 与 WXSS 于 23:07:30 更新，SHA-256 分别为 `4FF3612E3CFCBC81405B216FB482920C351EDCFB7CE6E955DEECD5E41841E3D8`、`8406A537844DD7795B1490D1BB77D2DED899DDB7294F426F101403B23EAF51BD` 和 `608B7960D6DD986D4F7126729D44495A78CD3C7A226351B1ABAD174AC2C3656E`；模拟器截图确认选中底色铺满半区且不存在独立内框，模板与样式编译通过、控制台无错误。23:10:44 以 `pages/appointment-create/index` 为入口重新执行 `auto_preview` 成功，包体为 `1919111` 字节；待用户确认真机效果后才能标记“真机已更新”。
- 2026-09-06：完成普通预约时的实际用量默认值改为优先使用预约创建时保存的预计用量快照；仅旧版空快照预约回退合并当前项目的正常用量，避免影响已有预约的预计库存语义。该逻辑覆盖详情页与列表快捷完成入口，并补充快照优先、空快照回退两项单元测试。`npm run type-check`、69 个测试文件共 384 条测试、`npm run build:mp-weixin` 和 `git diff --check` 均已通过；模拟器已重新编译 `pages/appointment-detail/index` 且控制台无错误。构建产物 `appointment-form-state.js`、`AppointmentCompletionForm.js`、`AppointmentDetailPage.js`、`AppointmentManagement.js` 的 SHA-256 分别为 `3622F0E89B84B93CA39760350390ED2799D80FCBC12A32425C3849988C57E060`、`A91193C6A1C17A1735E492BF666329301486B8F74AD1345BE95E63429A29DC7D`、`9717633B1B1EA03850F9E05606D64A4489658B790B413C4F0881080C118F7EED`、`AC3DEE4760419F891EFB5F9EC7359C60EEBA72679CE4CC091DDC406EBDF7F528`。预览包推送需向微信开发者账号外发私有代码，本次未获单独的安全放行，尚待用户在真机确认。
- 用户于 2026-09-06 明确授权后，预约详情页入口 `pages/appointment-detail/index` 的私有预览包已成功推送至当前登录的微信开发者账号，包体为 `1921786` 字节；等待真机确认“完成预约”弹层默认用量。

- 预约分段控件第三次修正：用户指出正常预约与其他选中项的勾选反馈不一致；默认选中的“正常预约”已补齐同款勾选图标，使正常预约、后补预约、已完成和未完成遵循同一选中规则。`AppointmentForm.js` 与 WXML 于 2026-09-05 23:29:30（UTC+8）更新，SHA-256 分别为 `71607454D6FA1886F8217A057F53E1E185A8D6AD01456C97D915740A15039DE3` 和 `CB2875E06451DA07C6AE746F656A3396629E303C93EC1DE87BE7ABED96EF7BEC`；Vue 类型检查、69 个测试文件共 379 条测试、微信发布构建、模板与样式编译均通过，模拟器截图确认默认正常预约已显示对号且控制台无错误。23:32:27 再次以 `pages/appointment-create/index` 为入口执行 `auto_preview` 成功，包体为 `1919540` 字节；待用户确认真机效果后才能标记“真机已更新”。

- 预约列表进一步收敛：状态数量只在待执行、已完成、已取消三个 Tab 中展示；不重复显示当前筛选结果数或“月历”入口，避免总数与搜索结果数混用，也避免入口与美容模块“日程”页重复。`AppointmentManagement.js` 与 WXML 于 2026-09-06 12:14:11（UTC+8）更新，SHA-256 分别为 `898FEBB70848C2E5568CA0CAF75D3EB2544815205521B89A71CA118C40A0B10C` 与 `CAD7416879AE1720B39D41C1BD57C5AA8F9C4D829DB2BD52BFD8DE3C8F47F2A8`；Vue 类型检查、69 个测试文件共 380 条测试、微信发布构建、`git diff --check` 均通过，模拟器控制台无错误。12:15 前以 `pages/appointment/index` 执行 `auto_preview` 成功，包体为 `1918002` 字节；当前仅表示预览包推送命令成功，待用户真机确认后才能标记“真机已更新”。

- 预约列表新增图标统一：主“新增”按钮的加号由误设的 `24px` 收敛为与顾客、项目和库存列表一致的 `14px`，文字、按钮高度和间距不变。`AppointmentManagement.js` 与 WXML 于 2026-09-06 12:17:41（UTC+8）更新，SHA-256 分别为 `8FBAE5021EC7A34D336FB14D05CE6D925FC8B4E12AB49CED4107C0FBBC455190` 与 `688C1AA924ED61229E3654CA0BC47676A436746478217002F3E560E9903A0470`；Vue 类型检查、69 个测试文件共 380 条测试、微信发布构建、`git diff --check` 均通过，发布产物明确包含 `name: "plus"` 和 `size: "14"`，模拟器控制台无错误。12:18 前以 `pages/appointment/index` 执行 `auto_preview` 成功，包体为 `1918002` 字节；当前仅表示预览包推送命令成功，待用户真机确认后才能标记“真机已更新”。

- 完成列表标签收敛：已完成 Tab 已经表达状态，普通完成卡片不再重复展示“已完成”标签；后补完成卡片只保留“后补”标识，避免丢失补录来源。`AppointmentList.js` 与 WXML 于 2026-09-06 12:24:40（UTC+8）更新，SHA-256 分别为 `40C0F2E79B5952A9E9E5303AE4B6CD9106B9302F56463EC38CAC865521C92481` 与 `8A286226474ACA3A3B7206C1EA35F6E26B90A7C0A3DC8F814D7462BE30CFEB68`；Vue 类型检查、69 个测试文件共 381 条测试、微信发布构建、`git diff --check` 均通过，WXML 仅在“非完成或后补”的条件下渲染卡片标签，模拟器控制台无错误。12:25 前以 `pages/appointment/index` 执行 `auto_preview` 成功，包体为 `1918258` 字节；当前仅表示预览包推送命令成功，待用户真机确认后才能标记“真机已更新”。

- 预约详情信息布局：预约信息各行改为左侧固定字段列和右侧字段值列，地址、项目组合和备注等长内容只在右侧换行。行距收紧，左列收为 `152rpx`，右侧字段值提升为 `27rpx`，使内容成为明确的视觉重点。顾客完整手机号显示为紫色浅底拨号按钮，保留电话图标、“拨打”提示及明确按压态，点击直接唤起系统拨号界面，失败时给出弱提示且不离开详情页。`AppointmentDetailPage.js` 与 WXML 于 2026-09-06 12:45:16（UTC+8）更新，SHA-256 分别为 `AD14697CD648E0DB0011DA0844433FFE98A05DED3D5B819BA16A1C48E03F24EC` 与 `B88748C48379919DE2D1C18F50866E8588E7205F6FE0129A381A36311628CB42`；Vue 类型检查、69 个测试文件共 382 条测试、微信发布构建、`git diff --check` 均通过，发布 WXSS 已确认行内间距 `18rpx`、上下留白 `12rpx` 和右侧 `27rpx` 字号，模拟器控制台无错误。12:46 前以 `pages/appointment-detail/index` 执行 `auto_preview` 成功，包体为 `1918988` 字节；当前仅表示预览包推送命令成功，待用户真机确认后才能标记“真机已更新”。

- 2026-09-14 预约详情补充手机号拨号能力：`AppointmentDetailPage.js`、WXML 与 WXSS 的 SHA-256 分别为 `F99D3D4B58090C17A0CD950701F96C08A8A01B090B6A359529AEB282BA34E382`、`86553548845EC7F8C84AAED95400F42406F64DA2F4B69E62152FA42C92471BDE` 和 `DA5E22B9478E3D5C25154E7C5910BCF7A4D8F598DBC90CDEE0CAFA887FDD0F53`。Vue 类型检查、72 个测试文件共 397 条测试、微信发布构建和 `git diff --check` 均通过，发布产物已确认包含 `uni.makePhoneCall`、失败提示、拨号按钮及按压态。22:19 以 `pages/appointment-detail/index` 生成新预览二维码成功；用户明确要求推送后，`auto_preview` 已成功上传同一构建包，包体为 `2060863` 字节。当前仅表示推送命令成功，待用户手机确认后才能标记“真机已更新”。

### “服务项目”当前方向

- `美容模块/27-服务项目-项目列表-v1.png` 已于 2026-09-04 确认定稿并完成代码实现，页面归属美容模块的业务子页面，延续浅粉背景、雾紫强调色和高不透明度实体列表卡片，不设置模块底部导航。
- 页面只保留原生导航栏中的“服务项目”标题；首行并排提供项目名称搜索与紧凑的“新增”按钮，按钮加号使用 `14px` 尺寸；下一行以结果数量和“仅看停用”复选框表达列表范围。全页图标按层级收敛为搜索/状态操作 `20px`、次级操作图标与箭头 `16px`、列表主新增加号与下拉提示 `14px`、表单“新增库存物品”加号 `12px`，不让图标抢过字段和操作文字。
- 每张项目卡片突出项目名称、标准价格和预计服务时长，并在分隔线下展示项目默认用量摘要；未配置默认用量时明确显示“未设置默认物品用量”。
- 点击项目卡片进入独立详情；编辑、停用和彻底删除等低频或风险操作不堆叠在列表卡片中。列表不展示项目照片、销量、成本、分类、折扣或库存预警等未定义信息。
- `美容模块/28-新增服务项目-独立表单-v1.png` 已于 2026-09-04 确认定稿并完成代码实现：原生导航栏承担“新增服务项目”标题与返回；第一张实体表单卡包含必填项目名称、标准价格和预计服务时长，并提供 30/60/90/120 分钟快捷项；第二张卡独立配置选填默认物品用量和快速新增库存物品，“新增库存物品”加号使用 `12px`；“保存项目”作为卡片外页面级主操作。
- `美容模块/29-项目详情-默认物品用量-v1.png` 已于 2026-09-04 确认定稿并完成代码实现：顶部项目概览常驻名称、启用状态、标准价格、预计服务时长和轻量“编辑资料”入口；默认物品用量使用独立实体卡逐项展示；停用与彻底删除收敛到页面底部的项目操作区，危险操作保持低频层级。详情页不设置无实际内容分区需求的 Tab。
- 编辑页复用新增页的表单结构，只切换标题、预填状态和保存文案，不另造视觉体系。新增/编辑表单保留快速新增库存物品的原草稿，并使用微信原生返回提示保护未保存修改和保存中的原子写入。

### “库存物品”当前方向

- `美容模块/22-物品详情-库存动态-v2.png` 已于 2026-09-04 确认定稿并完成代码同步：库存列表不再承载全局“最近变动”，点击物品进入独立详情后查看该物品自己的只读库存动态；详情顶部常驻当前库存、占用、可用库存与补货/盘点修正操作，下方通过“物品资料 / 库存动态”双 Tab 分隔资料维护和按时间倒序的变动时间线，首次进入默认展示库存动态。动态不提供单条编辑或删除，手工录错通过新增盘点修正纠正。未来若需要全局流水，可直接汇总现有库存变动记录，不改变数据结构。
- `美容模块/21-库存物品-库存列表-v1.png` 是已确认定稿，已完成代码实现；页面归属美容模块的业务子页面，延续浅粉背景、雾紫强调色、鼠尾草绿数值和高不透明度实体列表卡片，不设置模块底部导航。
- 页面只保留原生导航栏中的“库存物品”标题；首行并排提供物品名称搜索与紧凑的“新增”按钮，新增按钮加号使用与服务项目列表一致的 `14px`；下一行以结果数量和“仅看停用”复选框表达列表范围。
- 每张库存物品卡片突出名称、计量单位以及“当前库存 / 占用 / 可用库存”三项数据；可用库存使用鼠尾草绿色强调，并提供进入物品资料的右箭头。
- “补货”和“盘点修正”作为卡片内轻量快捷操作，不展示采购单价、成本、图片、缩略图或未经需求定义的库存预警。
- `美容模块/23-补货-库存增加-v2.png` 是“补货”独立页面的已确认定稿并已完成代码实现；原生导航栏承担页面标题与返回，库存概览统一展示物品和当前库存，表单卡不再重复“补货 + 物品名 + 当前库存”和“取消”，直接从“补货增加”选中态开始，再依次展示本次增加数量、正向差额和确认补货主操作。
- `美容模块/24-盘点修正-实际库存-v2.png` 是“盘点修正”独立页面的已确认定稿并已完成代码实现；原生导航栏承担页面标题与返回，库存概览统一展示物品和当前库存，表单卡不再重复“盘点 + 物品名 + 当前库存”和“取消”，直接从“盘点修正”选中态开始，再依次展示盘点后的实际库存、自动计算的差额和确认盘点修正主操作。
- `美容模块/25-编辑物品资料-独立表单-v4.png` 是“编辑物品资料”独立页面的已确认定稿并已完成代码实现；原生导航栏统一承担页面标题与返回，资料卡不再重复展示“编辑物品资料”和“取消”，直接从资料编辑提示开始；资料卡在备注字段后结束，“保存修改”作为页面级主操作独立放在卡片下方。不重复展示已经位于物品详情“物品资料”Tab 内的停用与彻底删除操作，也不混入库存数量调整。
- `美容模块/26-新增库存物品-独立表单-v1.png` 是“新增库存物品”独立页面的已确认定稿并已完成代码实现；它复用已确认编辑页的页面结构与视觉语言，由原生导航栏承担标题和返回，表单卡内不重复标题或“取消”。新增场景依次提供物品名称、计量单位、计量方式、首次库存和备注字段，并提示首次库存会生成入库记录；“保存物品”作为页面级主操作独立放在卡片下方。普通新增和项目快速新增复用同一路由，分别返回库存列表和原项目草稿。
- 新增物品独立页实现状态：新增 `pages/inventory-create/index` 路由，库存列表“新增”和服务项目快速新增均进入该页；快速新增使用一次性来源标识，只在原项目页仍位于页面栈中时回传并自动选中新物品，外部、过期和根页面旧链接降级为普通新增。表单草稿、保存中离页和组件卸载后的异步完成统一使用库存表单生命周期保护。Vue 类型检查、65 个测试文件共 351 条测试、`git diff --check` 和微信发布构建均通过；最终构建开始时间为 2026-09-03 22:28:54（UTC+8），`InventoryItemCreatePage.js` 与 `quick-add-inventory-handoff.js` 更新时间均为 22:29:10，SHA-256 分别为 `275D2CCFC62758A518AD275593CC3650126C56F7E53396C945C87C90ACFDC290` 和 `3123E1E11A5F676B71F9ABE9A24CF628FA27A83401D4CAB09971616858D3A3F5`。首次预览上传暴露微信工具仍缓存已删除的临时导航模块；核对磁盘无残留引用后关闭并重开同一个 build 窗口、清理 `cleanCompileCache`，模拟器再次确认标题、五个字段、卡片外保存按钮完整显示且控制台无错误。22:34（UTC+8）以 `pages/inventory-create/index` 为入口执行 `auto_preview` 成功，包体为 `1763199` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机已显示独立新增页后才能标记真机已更新。
- 真机保存按钮修正：用户反馈上述预览中“保存物品”显示异常，本轮状态立即记为“真机未更新”。第一轮通过明确高度、宽度、内边距和 flex 居中排除布局偏差，用户截图进一步确认实际问题是文字后方出现微信原生 `button` 内部矩形色块。因此最终主操作改用无原生默认层的 `view role="button"`，保留整块点击、提交中拦截、`aria-disabled`、明确禁用态和轻量按压反馈。Vue 类型检查、65 个测试文件共 351 条测试、微信发布构建、按钮样式契约和模拟器验证均通过；最终 `InventoryItemForm.wxml` 和 `.wxss` 更新时间为 2026-09-03 22:42:55（UTC+8），SHA-256 分别为 `12E2998B51A99F65B73CA50F56D4905A731F2B4F9079CF3E2F43CB85D2BD856C` 和 `FDA7B1FF09C41C4B6AE00EA79339AE83E96319B1CEAEEF46539E2FF7AEC2829B`。模拟器确认文字后无矩形色块且控制台无错误；22:44（UTC+8）以 `pages/inventory-create/index` 为入口执行 `auto_preview` 成功，包体为 `1763199` 字节。用户随后反馈认为真机没有收到该版本，当前明确记为“真机未更新”，正在重新构建、清缓存并生成新的扫码预览；再次得到用户确认前不得标记真机已更新。
- 三张独立子页实现状态：新增 `pages/inventory-adjustment/index` 和 `pages/inventory-profile-edit/index` 路由，列表与详情入口均使用独立页面；调整页实时展示变动前、差额和变动后，资料页按项目、预约和历史预约消耗统一锁定不可修改的计量单位。有草稿时原生返回会确认放弃，保存中返回会提示写入仍将完成，组件卸载后的异步结果不会再次触发导航。Vue 类型检查、64 个测试文件共 346 条测试、`git diff --check` 和微信发布构建均通过；构建开始时间为 2026-09-03 21:10:02（UTC+8），`InventoryAdjustmentPage.js` 更新时间为 21:10:10，SHA-256 为 `CD9DF45C30C5E333389F54D3629D3BA31A34D8A5392E05BDC3C08568E6EB9F86`，`InventoryItemProfileEditPage.js` SHA-256 为 `7728D53D60E74AA083C03EC389808AE5198228B4F74F3299C00B51F78A9231E2`。模拟器使用一条随后彻底清除的测试物品逐页确认“补货 / 盘点修正 / 编辑物品资料”的原生标题、单一库存概览、去重表单与卡片外保存按钮，控制台无错误。21:12（UTC+8）以 `pages/inventory/index` 为入口执行 `auto_preview` 成功，包体为 `1745468` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机已显示三个独立页面后才能标记真机已更新。
- 库存列表首次实现验证（历史）：Vue 类型检查、62 个测试文件共 338 条测试和微信发布构建已通过；build 目录的库存页面 WXML/WXSS 只读编译成功。模拟器首次发现独立纯 TS 筛选模块未进入微信主包依赖图并导致白屏，将筛选逻辑并入稳定装载的库存 composable 后重建、重开 build 窗口并清理 `cleanCompileCache`，模拟器已确认原生标题“库存物品”、名称搜索、紧凑新增入口、结果数量、“仅看停用”和空状态正常显示，控制台无错误。独立代码审查后又补齐提交期间的表单字段锁定，并在加载和保存刷新期间持续保留列表实例，避免搜索、停用筛选与变动分页丢失。最终构建开始时间为 2026-09-02 21:40:19（UTC+8），`InventoryItemCard.wxss` 更新时间为 21:40:37，SHA-256 为 `C4AEF9834E07448ED5FF188A54C64B33A9AB3C7FDBC34B1090FA8C5390CA5CD1`。2026-09-02 21:47（UTC+8）经用户明确授权，以 `pages/inventory/index` 为入口执行 `auto_preview` 成功，包体为 `1667553` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机已显示本轮库存物品页面，尚不能标记真机已更新。
- 真机预览补充诊断：2026-09-02 用户反馈上述自动推送在手机未显示，本轮状态明确记为“真机未更新”。重新执行发布构建后，开发者工具一度只显示原生标题和空白内容区；检查发现 `dist/dev/mp-weixin` 与 `dist/build/mp-weixin` 两个项目窗口同时打开且使用同一 AppID。22:01（UTC+8）关闭两个窗口、仅重开最新 build、清理 `cleanCompileCache` 并再次进入 `pages/inventory/index` 后，模拟器已稳定显示搜索、新增、结果数量、仅看停用和空状态，确认本次空白由同 AppID 的 dev/build 预览来源串用引起。22:02 基于该唯一 build 和 `pages/inventory/index` 入口生成新的窗口扫码二维码成功，包体为 `1667798` 字节；等待用户扫码确认，扫码反馈前仍不得标记“真机已更新”。
- 物品详情实现状态：新增 `pages/inventory-detail/index` 独立路由，列表卡片进入详情；详情只筛选当前 `inventoryItemId` 的库存动态，保留资料维护、补货、盘点修正以及预约消耗来源入口。2026-09-04 已完成库存动态只读改造，编辑/删除入口、内嵌编辑表单及手工动态重放业务能力均已移除；手工录错统一通过新增盘点修正纠正。库存列表返回显示时会读回详情操作后的最新库存，同时保留列表组件内搜索和停用范围状态。2026-09-05 将“物品资料”中的停用/重新启用与彻底删除操作补齐左侧 `20px` 状态/垃圾桶语义图标，并将右侧箭头统一为 `16px`，对齐服务项目详情的操作层级。
- 库存动态只读验证：2026-09-04 21:19:50（UTC+8）完成发布构建；`InventoryMovementList.wxml` 更新时间为 21:19:58，SHA-256 为 `05E7ADBE70101CB7BA084CEB0812B5FDFC5784FA0CEBA1FA885F73630EFDF323`。磁盘产物确认时间线仅保留库存事实与预约来源入口，不含单条编辑/删除事件；Vue 类型检查、65 个测试文件共 342 条测试、微信发布构建和 `git diff --check` 均通过。微信工具成功打开并刷新 `pages/inventory/index`，但自动化运行时持续返回空页面元数据，因此未把模拟器截图作为通过证据。用户明确授权上传本轮 build 后，首次 `auto_preview` 因工具仍缓存已删除的 `InventoryMovementEditForm.js` 依赖失败；关闭并重开 build 项目窗口、清理 `cleanCompileCache`、重新编译入口页后，于 21:31（UTC+8）再次执行 `auto_preview` 成功，包体为 `1737112` 字节，产物指纹保持不变。用户随后选择 `1`，确认真机库存动态已不再显示“编辑 / 删除”，本轮状态更新为“真机已更新”。
- 物品详情真机预览上传：2026-09-03 重新完成 Vue 类型检查、63 个测试文件共 343 条全量测试、`git diff --check` 和微信发布构建。成功构建开始时间为 16:38:37（UTC+8），`InventoryItemDetailTabs.wxml` 更新时间为 16:38:44，SHA-256 为 `9453C77468BF6DB465D1EA096D577B5D0EDEA3D735A0C7E8B259BBE90CC342A8`；产物包含 `pages/inventory-detail/index` 路由、“物品资料 / 库存动态”双 Tab 与库存列表到详情的导航。关闭异常窗口后仅重开 build 项目并清理 `cleanCompileCache`，模拟器运行时确认当前页为 `pages/inventory/index`、标题“库存物品”，实际页面包含名称搜索、紧凑新增入口、结果数量、“仅看停用”和空状态，控制台无错误。19:50:43（UTC+8）以 `pages/inventory/index` 为入口执行 `auto_preview` 成功，包体为 `1715772` 字节；用户随后确认补货、盘点修正和编辑资料三个入口均已分别生效，本轮状态更新为“真机已更新”。

### “顾客管理”当前方向

- `美容模块/17-顾客管理-顾客档案列表-v6.png` 是已确认定稿，页面归属美容模块的系统级子页面，延续浅粉背景、雾紫强调色和鼠尾草绿状态色，不设置模块底部导航，已完成代码实现。
- 页面只保留原生导航栏中的“顾客管理”标题，不再重复展示模块眉题、大标题和说明；首行并排提供昵称/手机号搜索与紧凑的“新增”按钮，搜索框占据主要宽度。
- 搜索行下方不设置全部/启用/停用分段筛选，也不重复展示“顾客资料”标题；左侧只显示当前结果数量，右侧提供“仅看停用”复选框。默认未勾选时只展示启用顾客，勾选后只展示停用顾客。
- 顾客卡片完整展示手机号、服务地址、累计完成次数和累计成交金额；使用高不透明度实体面板，不使用顾客头像、照片或图片占位。
- 顾客卡片内部不重复显示“启用”或“停用”标签；停用顾客使用低饱和灰色整卡与昵称删除线表达状态，其他信息仍保持可读。
- 点击顾客卡片进入详情，将编辑、停用和彻底删除等低频或风险操作收敛到详情。
- 验证状态：类型检查、304 条全量测试、微信发布构建和 build 目录模拟器顾客页均已通过；2026-08-31 21:52（Asia/Shanghai）已将 `pages/customer/index` 的预览包推送至开发者微信，用户已确认真机显示本轮“新增”按钮尺寸与文案，真机已更新。

### “新增顾客”当前方向

- `美容模块/18-新增顾客-独立表单-v2.png` 是已确认定稿，页面归属美容模块的独立子页面，延续顾客管理的浅粉背景、雾紫强调色和高不透明度实体表单面板，已完成代码实现。
- 原生导航栏标题使用“新增顾客”，左上角返回顾客管理；页面不保留顾客列表、搜索或筛选内容。
- 表单只包含必填昵称、必填中国大陆 11 位手机号，以及选填的多项服务地址；每项地址包含地址正文和地址备注，不设置默认地址。
- “添加地址”为次级描边操作，“保存顾客”为页面底部全宽主操作；不设置模块底部导航。
- 验证状态：类型检查、313 条全量测试和微信发布构建已通过；build 目录模拟器已核对 `pages/customer-create/index`、原生标题“新增顾客”和定稿表单布局，2026-09-01 用户已确认本轮真机显示正常。

### “编辑顾客”当前方向

- `美容模块/20-编辑顾客-复用独立表单-v1.png` 是已确认定稿；它复用已定稿的“新增顾客”独立表单布局与视觉语言，只切换页面模式和已有资料预填状态，已完成代码实现。
- 原生导航栏标题使用“编辑顾客”；昵称、手机号和服务地址带出当前顾客资料，底部主操作改为“保存修改”。
- 页面不增加头像、状态标签、取消按钮、底部导航或新的资料字段；返回时回到当前顾客详情，存在未保存修改时继续使用放弃确认保护。
- 2026-09-02 用户确认该稿定稿并开始开发；统一表单路由、编辑资料预填、创建/更新分流、缺失顾客恢复和详情返回刷新已实现。代码审查后又补齐保存竞态保护：提交期间全部输入和地址操作只读，保存中的原生离页提示明确说明写入仍会完成，页面卸载后不再触发第二次返回，写入完成事件会刷新仍在页面栈中的列表或当前详情。Vue 类型检查、60 个测试文件共 331 条测试、微信发布构建、顾客表单 WXML/WXSS 只读编译均通过。build 模拟器首次发现新增纯 TS composable 未进入微信主包依赖图，页面白屏；将编辑状态合并到已稳定装载的顾客 composable 后重建并清理 `cleanCompileCache`，模拟器已确认新增页标题及空白表单正确，编辑页标题为“编辑顾客”、测试顾客昵称和手机号已预填、无地址状态可读、底部操作为“保存修改”，控制台无错误。最新 `CustomerEditor.js` SHA-256 为 `19AFCC379D5B4C1D37EF1A1DCEC5074E725E271CE1F7ABE5611E7F609508D891`，`CustomerForm.js` SHA-256 为 `1DD26AAE285928A085F64C823CBFC878ACF2B6FFE7F64D7FC07FC79F454D06A3`。基于该产物，以 `pages/customer-create/index` 和测试 `customerId` 执行 `auto_preview` 成功，完成时间为 2026-09-02 17:12（UTC+8），包体 `1655148` 字节；当前仅表示本轮预览包推送命令成功，待用户确认手机已显示“编辑顾客”、预填资料和“保存修改”，尚不能标记真机已更新。当前尚未提交或推送。

### “顾客详情”当前方向

- `美容模块/19-顾客详情-双Tab档案-v3.png` 是已确认定稿，页面归属美容模块的独立子页面，延续顾客管理与新增顾客页的浅粉背景、雾紫强调色、鼠尾草绿状态色和高不透明度实体面板，已完成代码实现；v3 以标准线性电话图标、完整号码和“拨打”组成浅雾紫轻量胶囊，使拨号能力更明确但不抢过“编辑资料”的层级。
- 原生导航栏标题使用“顾客详情”，左上角返回顾客管理，不设置模块底部导航；顶部顾客资料与累计数据常驻，下方使用“顾客资料 / 历史预约”两个 Tab 分隔内容。
- 顾客资料完整展示昵称、启用状态和手机号；手机号使用“电话图标 + 完整号码 + 拨打”的浅雾紫胶囊与按压反馈表达可点击，点击后由用户主动唤起系统拨号界面；编辑资料使用轻量次级操作，累计完成次数与累计成交金额沿用列表口径并突出数值。
- 默认选中“顾客资料”，其中依次展示服务地址与资料操作；“历史预约”Tab 带预约数量，切换后展示预约项目组合、状态、时间、金额、预计时长和查看详情入口。
- 2026-09-17 历史预约卡片完成可读性与交互修正：项目组合提升为 `30rpx` 主标题，预约时间、时长、金额与结果信息提升至 `22–26rpx`，时长和金额使用浅雾紫摘要区分组；整张卡片带明确按压态，点击后进入对应预约详情页，移除顾客详情内重复展开的项目、地址和用量明细。
- 本轮顾客历史预约修正已通过 Vue 类型检查、2 个相关测试文件共 9 项回归测试、微信发布构建与 `git diff --check`；完整测试共 407 项，其中 406 项通过，唯一失败为工作区既有 `vite.config.ts` 小程序压缩配置与旧源码契约断言不一致，与本轮改动无关。构建开始于 2026-09-17 21:52:35（UTC+8），`CustomerAppointmentHistory.js` 于 21:53:08 更新，SHA-256 为 `8291F8A72E32DB23C21787F96449D44746995C49E8CCFA772B15E095B319B947`；发布 WXML 已确认整卡 `bindtap` 上抛预约标识，路由产物已确认进入 `pages-beauty/appointment-detail/index`。build 项目中的 WXML/WXSS 只读编译成功，模拟器控制台 error 过滤为空；模拟器当前没有顾客数据，因此未把空列表截图作为卡片视觉通过证据。`auto_preview` 因外部上传安全审批未获放行而未执行，本轮预览包尚未推送，真机效果待确认。
- 用户于 2026-09-17 明确选择推送后，21:58（UTC+8）使用上述同一构建、以 `pages-beauty/customer/index` 为入口执行 `auto_preview` 成功；微信工具返回总包 `2079572` bytes、主包 `1279323` bytes、美容分包 `800249` bytes，产物指纹保持为 `8291F8A72E32DB23C21787F96449D44746995C49E8CCFA772B15E095B319B947`。当前仅表示本轮预览包推送命令成功，待用户手机确认历史预约卡片的新样式与详情跳转后才能标记“真机已更新”。
- 用户随后选择“样式和详情跳转都正常”，确认手机已显示本轮历史预约卡片的新字号、信息层级与整卡详情跳转；本轮状态更新为“真机已更新”。该确认不等于上传体验版或发布正式版。
- 停用和彻底删除收敛到“顾客资料”Tab 末尾的独立低频操作区；启停/重新启用使用左侧 `20px` 状态图标，彻底删除使用左侧 `20px` 垃圾桶图标，右侧方向箭头统一为 `16px`，与服务项目、物品详情保持一致。彻底删除使用克制的危险色，不与查看资料的主层级竞争。页面不使用顾客头像、照片或图片占位。
- 验证状态：Vue 类型检查、61 个测试文件共 326 条测试和微信发布构建已通过；build 目录模拟器已使用带 `customerId` 的 `pages/customer-detail/index` 核对详情组件能够真正加载，模块缺失白屏已转为明确的顾客不存在恢复态，控制台无错误；磁盘产物已核对双 Tab、服务地址、资料操作及缺参返回入口。2026-09-01 21:24（UTC+8）重建微信项目依赖图后，以 `pages/customer/index` 为入口执行 `auto_preview` 成功，`useCustomerManagement.js` 产物 SHA-256 为 `306B64518C7B7531CC111842F4521632315E18DF035FDD68AE486D6A0181D380`；仅表示本轮白屏修复预览包推送命令成功，待真机确认。2026-09-01 21:57（UTC+8）将“编辑资料”按钮改为固定 `68rpx` 高度、取消上下内边距并使用 flex 垂直居中；微信工具只读编译最新 `CustomerDetailProfile.wxss` 成功、模拟器控制台无错误，随后再次以 `pages/customer/index` 执行 `auto_preview` 成功，包体 `1629419` 字节，产物 SHA-256 为 `96313596CCD0957E9A1EBBA0ADC6DB38BB4E0429E2F76C65F882D9C59F410F4E`；仅表示本轮按钮修正版预览包推送命令成功，待真机确认。2026-09-01 22:06（UTC+8）完成手机号点击拨号交互；发布产物已核对 `bindtap`、`uni.makePhoneCall` 和失败提示，微信工具只读编译详情 WXML/WXSS 成功，build 模拟器打开详情路由且控制台无错误，随后以 `pages/customer/index` 执行 `auto_preview` 成功，包体 `1629419` 字节，`CustomerDetail.js` 产物 SHA-256 为 `9122D0E2483B3C742E644B5A5ED059FA77DFCF2622EA4BA8CEC1486A0B8EC72D`；仅表示本轮拨号交互预览包推送命令成功，待真机确认。
- 真机补充验证：2026-09-01 22:07（UTC+8）用户反馈手机未看到拨号交互，状态明确记为“真机未更新”。随后关闭并重开 build 项目窗口、清理 `cleanCompileCache`、重新编译详情路由，磁盘产物确认事件链为手机号 `bindtap` → `call` → `bindcall` → `uni.makePhoneCall`，模拟器控制台无错误。22:11 基于同一 SHA-256 为 `9122D0E2483B3C742E644B5A5ED059FA77DFCF2622EA4BA8CEC1486A0B8EC72D` 的产物重新生成预览二维码并执行 `auto_preview`，两次返回包体均为 `1630704` 字节，与清缓存前返回的 `1629419` 字节不同；仅表示新的预览包生成和推送命令成功，待用户扫码或重新进入后确认，当前仍不得标记“真机已更新”。
- v3 电话胶囊验证：2026-09-01 22:36（UTC+8）按确认图完成“电话图标 + 完整号码 + 拨打”的浅雾紫胶囊，并将 `phone` 纳入本地 `AppIcon` 精简字体。Vue 类型检查、61 个测试文件共 326 条测试和微信发布构建通过；清理 build 编译缓存后，详情 WXML/WXSS 只读编译成功、详情路由可打开且模拟器控制台无错误。`CustomerDetailProfile.wxss` SHA-256 为 `138F788B1C5D63656331CAFAE0B47D563B545C477746BBF3D21558DD71C61A6D`，`zhuangyue-icons.ttf` SHA-256 为 `BD66A58E18421FB6B5C44F7EF1AABEE804726A9994D6B8A566A30555528BFE95`。生成新预览二维码的外部上传动作被安全审批拦截，v3 新包尚未上传微信，等待用户明确授权后继续；当前真机仍未更新。
- v3 预览上传：2026-09-01 22:40（UTC+8）用户明确授权将 v3 私有构建上传到微信开发者服务后，以 `pages/customer/index` 为入口生成新预览二维码并执行 `auto_preview`，两次均返回成功且包体一致为 `1633198` 字节；二维码已在电脑窗口显示，自动推送命令已完成。此状态仅表示同一 v3 预览包生成和推送成功，待用户确认手机出现电话图标、完整号码和“拨打”胶囊后才能标记“真机已更新”。
- Tab 指示条真机修复：2026-09-01 22:46（UTC+8）确认原实现复用了微信原生 `button::after`，开发者工具与真机对该伪元素的布局处理不同，导致选中指示条可能覆盖文字。现已改为按钮内部独立 `view` 并固定在 Tab 底边；先建立会失败的源代码回归约束，再修复至通过。Vue 类型检查、61 个测试文件共 326 条测试、微信发布构建、详情 Tab WXML/WXSS 只读编译及模拟器控制台检查均通过；最新 `CustomerDetailTabs.wxss` SHA-256 为 `C3E7F2E42C08BE40EE6646792CE527F290F9BFF70C9C1607E663FCA463134E32`。本轮新 payload 的 `auto_preview` 外部上传因需要单独明确授权而未执行，当前待用户授权推送，不能标记真机已更新。
- Tab 修复预览上传：2026-09-01 22:52（UTC+8）用户选择 `1`，明确授权上传本轮 Tab 指示条修复后的私有预览包；再次核对构建产物包含独立 `.customer-tabs__indicator` 且不再包含活动 Tab 的自定义 `::after` 后，以 `pages/customer/index` 为入口执行 `auto_preview` 成功，包体为 `1633614` 字节，`CustomerDetailTabs.wxss` SHA-256 仍为 `C3E7F2E42C08BE40EE6646792CE527F290F9BFF70C9C1607E663FCA463134E32`。此状态仅表示本轮预览包推送命令成功，待用户确认指示条已位于文字下方后才能标记真机已更新。
- uview-plus 电话图标替换：2026-09-01 23:08（UTC+8）按用户选择将顾客详情电话图标改为 uview-plus 标准 `u-icon name="phone"`，并从项目本地 `AppIcon` 精简字体中移除本轮新增的 `phone` 字形；项目 UI 交付技能同步规定“新增或本轮触及的通用功能图标优先使用 uview-plus，缺少合适语义或定稿要求专属造型时再使用 `AppIcon`”，不批量迁移未涉及页面。Vue 类型检查、61 个测试文件共 326 条测试、微信发布构建、顾客资料 WXML/WXSS 只读编译和模拟器控制台检查均通过；构建产物已核对 `u-icon` 组件依赖，`CustomerDetailProfile.wxml` SHA-256 为 `0FF95AB2D6ADE9912C2ABDDF06EC1FC185FBD004C3C38F71D508E77BB4C949CE`。官方技能校验脚本因本机没有可用 Python 运行时无法启动，已人工核对 frontmatter、技能名称、描述、规则存在性和占位符。新图标 payload 的 `auto_preview` 外部上传因需要单独明确授权而未执行，当前待用户授权推送，不能标记真机已更新。
- uview-plus 图标预览上传：2026-09-01 23:11（UTC+8）用户选择 `1`，明确授权上传本轮 uview-plus 电话图标私有预览包；上传前再次核对 `CustomerDetailProfile.wxml` 包含 `u-icon` 且组件依赖指向 `uview-plus/components/u-icon/u-icon`，随后以 `pages/customer/index` 为入口执行 `auto_preview` 成功，包体为 `1644140` 字节，`CustomerDetailProfile.wxml` SHA-256 仍为 `0FF95AB2D6ADE9912C2ABDDF06EC1FC185FBD004C3C38F71D508E77BB4C949CE`。此状态仅表示本轮预览包推送命令成功，待用户确认手机显示新的标准电话图标后才能标记真机已更新。
- uview-plus 图标真机反馈：2026-09-01 23:13（UTC+8）用户反馈手机“并没有”显示本轮预期效果，状态明确记为“真机未更新/新图标未确认”。后续须基于重新核验的同一构建生成可扫码预览二维码，区分手机仍在旧预览包与 uview-plus 图标字体真机加载异常；再次得到用户真机确认前不得标记真机已更新。
- uview-plus 图标扫码诊断：2026-09-01 23:15（UTC+8）重新执行微信发布构建，关闭复用的 build 项目窗口后重开并清理 `cleanCompileCache`；顾客资料 WXML/WXSS 只读编译成功、模拟器控制台无错误，`CustomerDetailProfile.wxml` SHA-256 仍为 `0FF95AB2D6ADE9912C2ABDDF06EC1FC185FBD004C3C38F71D508E77BB4C949CE`。随后基于该产物生成新的窗口扫码二维码成功，包体为 `1650629` 字节，入口为 `pages/customer/index`；等待用户扫码后确认手机是“仍显示旧图标”“图标空白”还是“已显示新图标”，当前仍为真机未更新/未确认。
- uview-plus 图标再次真机推送：2026-09-01 23:21（UTC+8）用户明确要求“推真机”，基于上述已重建、重开窗口、清缓存并完成只读编译检查的同一产物，再次以 `pages/customer/index` 执行 `auto_preview` 成功；返回包体与扫码二维码一致为 `1650629` 字节，`CustomerDetailProfile.wxml` SHA-256 仍为 `0FF95AB2D6ADE9912C2ABDDF06EC1FC185FBD004C3C38F71D508E77BB4C949CE`。此状态仅表示推送命令成功，待用户确认手机实际显示结果，当前仍不得标记真机已更新。
- uview-plus 图标真机确认：2026-09-01 23:23（UTC+8）用户反馈“可以了”，确认上述包体 `1650629` 字节的预览版本已在手机显示预期电话图标，本轮状态更新为“真机已更新”。

## 组件、图片与图标策略

- 运行界面可在页面设计或业务需求明确时使用本地图片、业务照片、缩略图、图片背景或 `<image>`；新增资源需兼顾包体、加载失败状态和隐私边界。
- 实现组件前先查 uview-plus；库内已有满足业务语义和交互要求的组件时优先复用，不重复自造同类组件。uview-plus 没有合适组件、真机表现不稳定或无法满足业务边界时，再实现聚焦的项目组件。本轮未涉及的既有页面不做批量迁移。
- 无论使用 uview-plus 还是项目组件，都必须适配当前页面既有的色彩、圆角、间距、排版和交互反馈，不直接带入与庄月空间现有页面冲突的组件库默认风格。
- 通用功能图标遵循相同优先级，优先使用 uview-plus 的 `<u-icon>`；缺少合适语义或定稿明确要求项目专属造型时，再使用 `AppIcon`。
- `AppIcon` 作为项目补充图标集，使用统一 1000 单位字形画布和组件行盒；字体文件、字符映射均由 `scripts/generate-icon-font.mjs` 生成，不同页面不得接触码位或各自修正图标位置。
- “我的”和系统级数据保护页的线条图标直接落在卡片内容区，不额外叠加彩色圆角底座；最近完整系统导出使用标准单圈时钟，不叠加回环、虚线尾巴或第二层圆环；从备份文件恢复使用折角文件内的向下箭头，不叠加回转圆弧。
- 六位模块授权码由项目适配层 `ModuleCodeInput` 统一接入 `uview-plus` 的 `up-code-input`。
- 高斯模糊、环境光、层次和装饰图形可继续优先使用 CSS；图片方案按具体页面效果和性能权衡决定。
- `ui图` 中的候选图默认用于开发评审；只有明确选作运行时资源时才进入小程序代码与构建产物。

## 视觉语言

- 工作台、“我的”及其系统级子页面使用上述统一的暖石色空间风格；模块管理 v1 与备份恢复 v1 均已确认并进入代码实现。
- 美容模块使用浅粉、雾紫和鼠尾草绿的轻美容风格；毛玻璃只用于概览卡、模块卡和弹层，表单与高频列表使用高不透明度实体面板。
- 当前业务列表和详情仍以文字信息布局为主；后续可按已确认需求增加产品图、包装图、顾客头像、照片、缩略图或媒体上传入口。
- 搜索、返回、日历等明确操作可以使用随应用打包的功能图标；图标不写入业务记录或备份。

## 当前图稿索引

### 13-工作台-模块门厅-无管理入口-v5.png

已确认并已进入代码实现的全局工作台定稿。相较 v4，移除“业务空间”标题右侧的“管理模块”入口。

### 12-我的-本机控制中心-v1.png

已确认并已进入代码实现的“我的”页面定稿。它包含本机存储状态、数据备份与恢复、模块管理、使用说明及“我的”导航选中态。

### 14-数据备份与恢复-本机数据保护-v1.png

“数据备份与恢复”页面的已确认定稿，已进入代码实现。它包含最近完整系统导出状态、完整系统备份、选择模块导出、从文件恢复和未加密文件提示。

### 15-模块管理-本机授权-v1.png

“模块管理”页面的已确认定稿，已进入代码实现。它包含当前已解锁的美容模块、六位模块授权码输入、“开启模块”操作和第一版不可移除提示；单模块场景不展示默认模块设置。

### 美容模块/16-美容管理-模块首页-v1.png

美容模块首页已确认定稿并已进入代码实现。它包含本月完成、本月成交、待执行、近期预约、四个业务入口及美容模块内部四页签导航。

### 美容模块/17-顾客管理-顾客档案列表-v6.png

“顾客管理”页面已确认定稿并完成代码实现。它只保留导航栏标题，将新增入口与昵称/手机号搜索并排展示，通过结果数量和“仅看停用”复选框表达列表范围，并移除顾客卡片内的状态标签。

### 美容模块/18-新增顾客-独立表单-v2.png

“新增顾客”独立页面已确认定稿并完成代码实现。它使用独立原生导航标题，提供昵称、手机号、多服务地址编辑和“保存顾客”主操作，不包含顾客列表内容或底部导航。

### 美容模块/19-顾客详情-双Tab档案-v3.png

“顾客详情”已确认定稿并完成代码实现。顶部常驻顾客资料与累计经营数据，手机号使用“电话图标 + 完整号码 + 拨打”的浅雾紫轻量胶囊，下方以“顾客资料 / 历史预约”双 Tab 分隔服务地址、资料操作和预约列表。

### 美容模块/20-编辑顾客-复用独立表单-v1.png

“编辑顾客”独立表单页的已确认定稿并已完成代码实现。它复用“新增顾客”定稿布局，展示已预填的昵称、手机号和服务地址，并将原生标题与底部主操作分别调整为“编辑顾客”和“保存修改”。

### 美容模块/21-库存物品-库存列表-v1.png

“库存物品”页面的已确认定稿并已完成代码实现。它使用名称搜索、紧凑新增入口、结果数量和“仅看停用”范围控制，并在实体卡片中展示名称、单位、当前库存、占用、可用库存及补货、盘点修正快捷操作。

### 美容模块/22-物品详情-库存动态-v2.png

“物品详情”页面的已确认定稿并已完成代码实现。它在顶部汇总当前库存、占用与可用库存，通过“物品资料 / 库存动态”双 Tab 展示当前物品资料和专属只读动态时间线；全局库存列表不追加跨物品流水，动态不提供单条编辑或删除。

### 美容模块/23-补货-库存增加-v2.png

“补货”独立页面已确认定稿并已完成代码实现。原生导航栏负责标题与返回，库存概览负责物品身份和当前库存；表单卡直接从“补货增加”选中态开始，填写本次增加数量，并以调整前、本次正向差额和调整后库存预览强化补货语义。

### 美容模块/24-盘点修正-实际库存-v2.png

“盘点修正”独立页面已确认定稿并已完成代码实现。原生导航栏负责标题与返回，库存概览负责物品身份和当前库存；表单卡直接从“盘点修正”选中态开始，填写盘点后的实际库存，并展示系统自动计算的差额与调整后数量。

### 美容模块/25-编辑物品资料-独立表单-v4.png

“编辑物品资料”独立页面已确认定稿并已完成代码实现。原生导航栏统一承担页面标题与返回，白色资料卡直接从提示和字段开始，不重复标题或“取消”；资料卡在备注字段后结束，“保存修改”作为页面级主操作独立放在卡片下方。停用与彻底删除继续只保留在物品详情“物品资料”Tab 中，避免重复入口。

### 美容模块/26-新增库存物品-独立表单-v1.png

“新增库存物品”独立页面的已确认定稿并已完成代码实现。它复用“编辑物品资料”的独立表单结构，由原生导航栏承担标题与返回；卡片内只保留新增提示及物品名称、计量单位、计量方式、首次库存和备注字段，“保存物品”作为页面级主操作放在卡片外，不展示取消、停用、删除或库存修正操作。普通新增和项目快速新增共用该页面，保存后按来源返回。

### 美容模块/30-预约执行-待执行列表-v4.png

“预约执行”列表页已确认定稿并完成代码实现。顶部提供顾客/项目搜索、统一新增入口和待执行/已完成/已取消状态筛选，卡片进入独立详情；待执行卡片保留完成预约快捷操作。

### 美容模块/31-新增预约-正常模式-v3.png

正常新增模式已确认定稿并完成代码实现。页面顶部默认选择正常预约，不展示实际用量；项目默认用量在后台自动形成预计占用。缺少启用顾客或启用服务项目时，空状态按当前缺失项显示对应说明和新增入口；两项都缺失时同时提供“新增顾客”和“新增服务项目”。

### 美容模块/32-预约详情-正常待执行-v3.png

正常待执行详情已确认定稿并完成代码实现。页面显示预计占用，并提供编辑、完成、改期、取消和彻底删除入口；完成预约打开当前页底部弹层。

### 美容模块/33-新增预约-后补完成-v3.png

后补完成模式已确认定稿并完成代码实现。后补模式默认已完成，实际用量默认带出项目正常用量，并与成交金额、实际完成时间一起原子保存。

### 美容模块/34-完成预约-实际用量弹窗-v1.png

完成预约底部弹层已确认定稿并完成代码实现。实际用量、成交金额、完成时间和备注在一个弹层内确认，选择库存物品后立即生成对应的用量行并填写数量，不需要额外点击“添加”；每条用量均可改选未被其他行占用的库存物品并保留已填数量；提交时金额、时间和数量错误就近显示，库存不足显示在实际用量区；成功后即时刷新预约详情或列表。

### 美容模块/35-新增预约-后补未完成-v1.png

后补未完成模式已确认定稿并完成代码实现。未完成等同顾客取消，取消原因必填；保存后直接成为带后补标识的已取消记录，不占用或扣减库存。

### 美容模块/36-预约月历-日程-v2.png

“日程”页已确认定稿并完成代码实现。页面位于美容模块内部“日程”页签，依赖原生导航标题显示“美容 · 日程”，页内不重复展示顶部身份文字或大标题；主体从月历卡片开始，月视图以状态圆点标记存在预约的日期，选中日期后展示当天预约列表，并可通过“管理预约”进入预约执行列表。

## 图稿自动维护规则

- 每个页面只长期保留一张已确认定稿；正在评审时，额外保留该页面最新一张候选稿。
- 美容模块图稿统一存放在 `ui图/美容模块/`；全局工作台、“我的”和系统级子页面图稿继续保留在 `ui图/` 根目录。
- 新候选稿生成后自动删除同页面已淘汰且未确认的旧版本；确认新稿后，用新稿替换旧定稿索引。
- 删除图稿前核对文件名和页面归属；`UI说明.md` 必须同步更新当前状态、关键决策和图稿索引。
- 图稿删除不代表删除需求；字段、状态和交互规则始终以需求文档和《交互说明.md》为准。

## 图稿未覆盖但第一版必须实现

- 顾客多地址管理与临时地址保存选项。
- 多项目选择、项目快速新增、库存物品快速新增。
- 完成确认、撤销完成、恢复取消、逾期和时间冲突警告。
- 本地容量占用、7MB 提醒和手动历史清理入口。

## 最新真机预览状态

- 2026-09-05 16:58:12（UTC+8）将库存物品列表“新增”按钮的加号由 `22px` 收敛为与服务项目列表一致的 `14px`，其余库存图标不变。发布构建开始于 16:56:14，`InventoryItemList.js` 与 WXML 更新于 16:56:26，JS SHA-256 为 `A2F508B291790FB1C169B225F3EFAB9FD894A7A9E050C11711DA9FAE7BF5A476`；磁盘产物与微信模拟器运行时均精确确认 `plus` 尺寸为 `14`。类型检查、68 个测试文件共 358 条测试、发布构建、列表 WXML 编译和 `git diff --check` 均通过，模拟器截图比例正常且控制台无错误。随后以 `pages/inventory/index` 为入口执行 `auto_preview` 成功，包体为 `1818648` 字节。用户随后选择 `1`，确认真机中的加号大小合适，本轮状态更新为“真机已更新”。
- 2026-09-05 16:52:06（UTC+8）将物品详情和顾客详情的资料操作区与服务项目详情统一：停用/重新启用使用左侧 `20px` 的 `pause-circle` / `play-circle`，彻底删除使用左侧 `20px` 的 `trash`，两个右侧 `arrow-right` 统一为 `16px`。最终发布构建开始于 16:49:48，两份 WXML 均更新于 16:50:00；`CustomerProfileDetails.wxml` 与 `InventoryItemProfileDetails.wxml` 的 SHA-256 分别为 `D7D2E4EB1BF18CB37982DAB9409EEFA866BA048F7DBF87A3ACBAD12C3261D7F8` 和 `A46A8817098A4A0980A14E357B67D527C98CBB11FB54D755B9D6206BB0E93919`。类型检查、68 个测试文件共 358 条测试、发布构建和 `git diff --check` 均通过；微信模拟器分别进入带真实本地标识的物品详情与顾客详情，运行时精确读取到上述图标和尺寸，截图布局正常，两份 WXML 编译成功且控制台无错误。随后以 `pages/customer/index` 为入口将包含两处改动的最终包执行 `auto_preview` 成功，包体为 `1818648` 字节。当前仅表示预览包推送命令成功，待用户确认手机效果后才能标记“真机已更新”。
- 2026-09-05 16:36:17（UTC+8）用户继续将新增/编辑服务项目表单“新增库存物品”的加号由试稿 `14px` 缩至 `12px`，列表主新增加号仍保持 `14px`，下拉箭头仍保持 `14px`。最终发布构建开始于 16:33:42，`BeautyProjectForm.js` 更新于 16:33:54，SHA-256 为 `8A742EEC4B7A60DE25A3840FC8E57FB5B0544BD68858331032C416A774486E55`，磁盘产物明确包含 `name: "plus"` 与 `size: "12"`。首次运行时断言仍读到上一版 `14`；再次清理 build 编译缓存、关闭并重开同一 build 窗口后，运行时精确读取到加号 `12`、下拉箭头 `14`，新增页截图正常，表单 WXML 编译成功且控制台无错误。类型检查、68 个测试文件共 357 条测试、发布构建和 `git diff --check` 均通过；随后以 `pages/beauty-project-create/index` 为入口执行 `auto_preview` 成功，包体为 `1816569` 字节。当前仅表示最终 `12px` 预览包推送命令成功，待用户确认手机效果后才能标记“真机已更新”。
- 2026-09-05 16:31:48（UTC+8）按用户反馈将新增/编辑服务项目表单“新增库存物品”的加号由 `16px` 缩至 `14px`；列表主新增加号继续保持 `14px`，其余图标层级不变。发布构建开始于 16:29:18，`BeautyProjectForm.js` 更新于 16:29:30，SHA-256 为 `C3B4B504E5F2BF02B7EF1DB828F56B7A5515D5F700057CCC0440C478BFEE8EF1`，磁盘产物明确包含 `name: "plus"` 与 `size: "14"`。首次运行时断言仍读到旧值 `16`，确认同 AppID 的 dev/build 缓存复用；清理 build 编译缓存、关闭并重开同一 build 窗口后，运行时重新读取为加号 `14`、下拉箭头 `14`，新增页截图正常，表单 WXML 编译成功且控制台无错误。类型检查、68 个测试文件共 357 条测试、发布构建和 `git diff --check` 均通过；随后以 `pages/beauty-project-create/index` 为入口执行 `auto_preview` 成功，包体为 `1816569` 字节。该试稿随后按用户要求由 `12px` 版本覆盖，不再作为待确认版本。
- 2026-09-05 16:22:18（UTC+8）按用户反馈将服务项目列表“新增”按钮的加号由 `16px` 缩至 `14px`，搜索图标 `20px`、次级操作图标与箭头 `16px` 等其余层级保持不变。发布构建开始于 16:18:29，`BeautyProjectList.js` 更新于 16:18:43，SHA-256 为 `6D057155E71E603C00CC623D3F697C30A049BD26457E7E5A5FB12FB265B63438`；磁盘产物明确包含 `name: "plus"` 与 `size: "14"`。清理 build 编译缓存并重开同一 build 窗口后，模拟器运行时精确读取到搜索图标 `20`、新增加号 `14`，页面截图正常，列表 WXML 编译成功且控制台无错误。类型检查、68 个测试文件共 357 条测试、发布构建和 `git diff --check` 均通过；随后以 `pages/beauty-project/index` 为入口执行 `auto_preview` 成功，包体为 `1816569` 字节。当前仅表示本轮预览包推送命令成功，待用户确认手机显示更小的加号后才能标记“真机已更新”。
- 2026-09-05 14:39:30（UTC+8）因用户反馈真机仍显示旧版，重新执行发布构建（开始 14:36:50，`BeautyProjectList.js` 更新于 14:37:02）、清理 build 项目的 `cleanCompileCache`、关闭并重开同一 build 窗口，再次打开 `pages/beauty-project/index`；列表 WXML 编译成功且模拟器控制台无错误。磁盘产物继续明确包含 `name: "plus"` 与 `size: "16"`，JS SHA-256 仍为 `0BE02E2A6B39A0D40BBD9655EAE4DBFD46BFCF974B6F79161F9FF2977E7B6F2B`。随后基于同一包生成新预览二维码并再次执行 `auto_preview`，两次工具均返回 `success: true`，包体均为 `1816569` 字节。当前仅表示二维码和重新推送命令成功，待用户通过新二维码或自动推送确认后才能标记“真机已更新”。
- 2026-09-05 14:34:56（UTC+8）按用户反馈将服务项目列表“新增”按钮的加号由 `18px` 进一步缩至 `16px`，其余图标层级不变，并以 `pages/beauty-project/index` 为入口重新推送到已授权账号“月庄”；工具返回 `success: true`，包体为 `1816569` 字节。最终构建开始于 14:33:22，`BeautyProjectList.js` 与 WXML 更新时间为 14:33:36，JS SHA-256 为 `0BE02E2A6B39A0D40BBD9655EAE4DBFD46BFCF974B6F79161F9FF2977E7B6F2B`，磁盘产物明确包含 `name: "plus"` 与 `size: "16"`。类型检查、68 个测试文件共 357 条测试、发布构建、列表 WXML 编译、`git diff --check` 和模拟器控制台检查均通过。用户随后反馈手机仍显示旧版，本轮状态更新为“真机未更新”，正在清理微信编译缓存并重新生成预览。
- 2026-09-05 14:30:44（UTC+8）以 `pages/beauty-project/index` 为入口，将服务项目全页图标层级收敛后的 build 预览包再次推送到已授权账号“月庄”，工具返回 `success: true`，包体为 `1816569` 字节。搜索/状态操作、主新增、次级操作/箭头和下拉提示分别统一为 `20px`、`18px`、`16px`、`14px`；构建开始于 14:28:20，相关 WXML 更新时间为 14:28:52。`BeautyProjectList.js`、`BeautyProjectCard.js`、`BeautyProjectForm.js`、`BeautyProjectActions.js` 的 SHA-256 分别为 `CB8A86343F91CFBD0405E76FA96915D460FA52EADF44925AE356455459D2091D`、`7CA63AE5559D8B11E42AD8D47D3857BF8FD1425DC20819BCDE02E9990C32A653`、`D510FED87C46F0E3BBA200DD9CBD5DB1EDE0EC24176081B09E230ECF2D3A827E`、`5D41AFA4E21605B92D0B1C9156F55CA257F8413476EB9BB5F6BB365DE95E9121`。类型检查、68 个测试文件共 357 条测试、发布构建、列表 WXML 编译、磁盘图标尺寸断言和模拟器控制台检查均通过。当前仅表示新预览包推送命令成功，待用户确认真机图标比例后更新状态。
- 2026-09-04 22:49:23（UTC+8）经用户明确授权后，以 `pages/beauty-project/index` 为入口将服务项目列表、独立新增/编辑表单和独立详情页的 build 预览包自动推送到当前登录账号“月庄”，工具返回 `success: true`，包体为 `1816569` 字节。发布产物中 `BeautyProjectForm.js`、`BeautyProjectDetailPage.js` 和 `beauty-project-navigation.js` 的 SHA-256 分别为 `C52B358E57ADC76B946D041AA95D2FCE24924C5738DE40B6F3A57AB8DC16A7F7`、`6667ADC79E0D4D15E45EE0DBFF574D5744511C4E5E36D1AA21A84F6DE9462986` 和 `B888F4E4B23DCF9925BCFCCC4C4F3A0076C6977E5747F510A2B827BC5446911D`。类型检查、68 个测试文件共 357 条测试、发布构建、关键表单 WXML/WXSS 编译和模拟器控制台检查均通过；模拟器因无可用本机业务数据只验证到可恢复的数据保护态。用户于 2026-09-05 确认真机已显示本轮新版服务项目页面，本轮状态更新为“真机已更新”。
- 2026-09-04 21:31（UTC+8）用户明确授权后，以 `pages/inventory/index` 为入口将库存动态只读改造的 build 预览包推送成功，包体为 `1737112` 字节；`InventoryMovementList.wxml` SHA-256 为 `05E7ADBE70101CB7BA084CEB0812B5FDFC5784FA0CEBA1FA885F73630EFDF323`。用户随后选择 `1`，确认真机库存动态已不再显示“编辑 / 删除”，本轮状态更新为“真机已更新”。
- 2026-09-04 用户已确认真机中的库存调整分段 Tab 和底部确认按钮显示正常，本轮盘点/补货控件修复状态更新为“真机已更新”。同一轮已推送包同时包含新增页“保存物品”和编辑页“保存修改”的无原生默认层按钮实现。
- 2026-09-04 20:22:32（UTC+8）完成盘点页 Tab 与确认按钮修正版发布构建；`InventoryAdjustmentForm.wxml` 和 `.wxss` 更新时间为 20:22:40，SHA-256 分别为 `E4B1A510C2DDF87402DE1D98FD667EB2A460E690AB4796686B22490B609D0C3F` 和 `F2271A5BA4D7BEE27D2F556F7C3115C1667FCD8F9DC5337C5E0D7117CE77EE85`。磁盘产物确认包含两个 `view role=\"tab\"` 和一个 `view role=\"button\"`，不含该表单的原生按钮；WXML/WXSS 只读编译成功。模拟器没有现成库存物品，临时分片直达验证触发仓储保护后已立即清理并恢复空索引，未将受保护状态作为视觉证据；库存列表重新打开正常且控制台无错误。Vue 类型检查、65 个测试文件共 351 条测试和 `git diff --check` 均通过；随后以 `pages/inventory/index` 为入口生成新二维码并执行 `auto_preview`，两次包体一致为 `1765943` 字节。当前仅表示新预览包生成和推送命令成功，待用户使用真机真实物品确认后才能标记真机已更新。
- 2026-09-04 用户反馈盘点修正页的分段 Tab 和“确认盘点修正”在真机存在内部色块。本轮将两者从微信原生 `button` 改为带 `tablist/tab/button` 语义的无默认层控件，统一固定高度、文字居中、选中态、提交中拦截和按压反馈；库存计算、表单字段和页面结构不变。当前正在验证，用户确认前状态为“真机未更新/待确认”。
- 2026-09-04 19:35（UTC+8）基于上述编辑页按钮产物，以 `pages/inventory/index` 为入口生成新窗口二维码并执行 `auto_preview`，两次返回包体均为 `1764484` 字节。当前仅表示二维码和自动推送命令成功，待用户从真机库存详情进入“编辑资料”确认“保存修改”无矩形色块后，才能标记真机已更新。
- 2026-09-04 19:29:11（UTC+8）完成编辑页按钮修正版发布构建；`InventoryItemProfileForm.wxml` 和 `.wxss` 更新时间为 19:29:41，SHA-256 分别为 `FB6211DBBD6450FE067BCF16256E9BC8F84C0875162281D2CBB30F0E2CBA3FD2` 和 `F9DCFD413D2322A45F9ECF8DA6FA3DED153113F854C6A7CA0C06B06469F385BD`。磁盘产物确认含 `view role=\"button\"` 且不含原生保存按钮；WXML/WXSS 只读编译成功，模拟器以一条验证后已清除的临时物品打开编辑页，确认“保存修改”整条渐变连续、文字居中且无矩形色块，控制台无错误。Vue 类型检查、65 个测试文件共 351 条测试和 `git diff --check` 均通过；待本轮 `auto_preview` 成功且用户真机确认后再更新真机状态。
- 2026-09-04 根据用户反馈，将编辑物品资料页“保存修改”同步改为与新增页一致的 `view role=\"button\"` 页面级主操作，统一固定高度、整宽渐变、文字居中、提交中拦截与按压反馈；不改变资料字段、计量单位锁定规则或物品管理入口。当前正在执行代码验证与新一轮真机预览，用户确认前状态为“真机未更新/待确认”。
- 2026-09-03 22:47:51（UTC+8）针对新增物品页保存按钮重新执行发布构建，`InventoryItemForm.wxml` 和 `.wxss` 更新时间为 22:47:58，SHA-256 分别为 `12E2998B51A99F65B73CA50F56D4905A731F2B4F9079CF3E2F43CB85D2BD856C` 和 `FDA7B1FF09C41C4B6AE00EA79339AE83E96319B1CEAEEF46539E2FF7AEC2829B`；WXML 确认为 `view role=\"button\"`，不再使用微信原生 `button`。
- 已关闭并重开同一个 build 窗口、清理 `cleanCompileCache`、重新打开 `pages/inventory-create/index`；WXML/WXSS 只读编译成功，模拟器控制台无错误。新窗口二维码生成与 `auto_preview` 再推送均成功，两次包体一致为 `1763850` 字节。
- 用户随后提供的截图中，整条渐变和按钮文字已连续显示、没有原生矩形色块；在用户明确该截图来自真机前，状态仍为“真机未更新/待确认”。

## 生成方式

现有图稿由 imagegen 生成，作为高保真视觉参考。后续美容模块图稿直接保存到 `ui图/美容模块/`，全局与系统级图稿保存到 `ui图/` 根目录，不在聊天中重复展示；应使用本文件中的新术语和字段，不再生成采购单价、头像、折扣编辑器、单项目预约或 Android 端页面。
