# 美容业务

本上下文描述美容服务经营过程中需要统一理解的业务对象，确保需求、界面和实现使用一致的语言。

## 库存

**库存物品**：
美容服务中会被持有、占用和消耗的一类物品；其当前库存代表此刻实际持有的数量，而不是某一次采购行为。第一版不保存采购单价，价格如有需要应属于具体补货记录。
_避免使用_：物品采购、采购物品、产品

**库存变动记录**：
一次导致库存物品实际数量增加或减少的事实记录，包括首次入库、补货、手工盘点和预约消耗；待执行预约的占用不属于库存变动。系统记录类型、变动前后数量、差额和时间，说明选填。个人手工产生的记录可以直接编辑或删除；预约产生的消耗记录只能通过更正对应预约来同步修改。
_避免使用_：采购记录、库存日志

**补货**：
按本次新增数量增加库存的操作，并生成相应库存变动记录。
_避免使用_：修改库存、再次采购

**盘点修正**：
直接填写盘点后的实际库存数量，由系统计算与原库存的差额并生成相应库存变动记录。
_避免使用_：补货、库存覆盖

**快速新增库存物品**：
配置项目或预约实际用量时就地创建并自动选中的库存物品，至少包含名称、计量单位和初始库存；返回后必须保留原有草稿。
_避免使用_：临时物品

**库存物品标识**：
启用状态下由名称与计量单位共同识别，同一“名称 + 单位”组合不可重复；停用对象与新对象冲突时，重新启用前必须先解决重名。
_避免使用_：仅物品名称

**可用库存**：
当前库存扣除其他待执行预约占用后的数量；任何库存或预约操作都不得使当前库存小于零或小于仍被占用的数量。
_避免使用_：当前库存、剩余库存

**计量单位**：
库存物品数量的计量口径；离散单位（如个、瓶、盒、片、支、次）只允许整数，连续单位（如毫升、克）允许最多两位小数。物品被项目或预约引用后单位不可直接修改，更换单位须停用旧物品并新建。
_避免使用_：规格

**停用业务对象**：
不再用于新业务但必须为历史记录保留的顾客、项目或库存物品；已被其他业务记录引用的对象只能停用，只有从未被引用的对象才能彻底删除。
_避免使用_：软删除、逻辑删除

## 服务项目

**预计服务时长**：
完成一个服务项目通常需要的时间，以分钟记录且必须填写；预约的具体日期和开始时间不属于项目时长。
_避免使用_：时间、项目时间、预约时间

**项目标准价格**：
服务项目通常收取的金额，必须明确填写且允许为零；零表示免费，未填写不表示免费。
_避免使用_：项目金额、默认金额

**项目默认用量**：
服务项目通常消耗的库存物品及其建议数量；项目可以不配置任何默认用量，配置时数量必须大于零并符合物品的计量精度。
_避免使用_：物品组合、项目库存

**快速新增项目**：
填写预约时就地创建并自动选中的服务项目，至少包含名称、项目标准价格和预计服务时长；返回预约后必须保留原有草稿。
_避免使用_：临时项目、无项目预约

**项目名称**：
启用状态下唯一的服务项目标识；停用项目与新项目重名时，重新启用前必须先解决冲突。
_避免使用_：重复项目名

## 预约执行

**预约项目组合**：
一次预约中计划提供的至少一个且互不重复的服务项目；其标准金额和预计服务时长分别由各项目快照相加，同一种库存物品的默认用量合并后形成该预约的本次用量。没有明确服务项目的预约不能保存。
_避免使用_：预约项目、单个项目

**预约实际用量**：
预约最终保存并用于库存占用和完成扣减的物品数量；项目默认用量仅用于初始化，用户可以在本次预约中添加物品、移除默认项或修改数量。预约项目组合一旦改变，已有实际用量全部作废并按新项目重新生成。
_避免使用_：项目默认用量、固定用量

**预约时间冲突**：
两个待执行预约按开始时间和预计总时长计算后发生重叠；系统必须警告但允许用户确认后继续保存，已取消预约不参与判断。第一版不计算上门路程或固定缓冲时间。
_避免使用_：重复预约

**预约服务地址**：
本次上门服务的必填地点快照，来源可以是顾客服务地址或临时服务地址；顾客地址后续变化不影响已经保存的预约。
_避免使用_：顾客当前地址

**取消原因**：
取消预约时可选择或填写的补充说明，属于选填信息，不得阻止用户完成取消操作。
_避免使用_：必填取消说明

**改期**：
直接修改原待执行预约的时间和服务地址，不取消原预约、不创建新预约，也不产生独立状态。
_避免使用_：已改期、取消后重建

**应用内预约提醒**：
用户进入小程序后看到的近期预约提示，首页包含全部逾期预约、今天的预约和未来三天的预约；第一版不包含小程序关闭后的微信消息通知。
_避免使用_：消息通知、系统通知

**逾期预约**：
计划开始时间已过但仍处于待执行状态的预约；逾期是自动计算的提示标记，不是独立状态，系统不得自动完成或取消预约。
_避免使用_：已过期状态、失效预约

**预约月历**：
按月查看哪些日期存在预约并进入某日预约列表的视图；第一版不包含拖拽改期、周视图或时间轴排班。
_避免使用_：排班日历、日程系统

**轻量经营统计**：
由预约直接汇总的完成次数、成交金额和待执行数量；第一版不记录支出，也不计算成本或利润。
_避免使用_：财务统计、利润报表、记账

**已完成预约**：
已经实际提供服务并扣减库存的预约。完成前必须确认实际物品用量、成交金额、实际完成时间和备注；完成后仍可更正这些内容，其中用量更正会同步补回或补扣库存，顾客和项目不能直接替换。
_避免使用_：执行记录、完成记录

**实际完成时间**：
服务实际完成的必填时间，确认完成时默认取当前时间并允许修改；经营统计按此时间归属，而不是按原计划时间归属。
_避免使用_：预约时间、更新时间

**撤销完成**：
将误标为已完成的预约恢复为待执行；系统补回原先扣减的库存，同时恢复该预约对相应用量的库存占用。
_避免使用_：删除完成记录

**恢复取消**：
将误取消的预约恢复为待执行；恢复前必须重新校验库存，库存足够时重新占用，库存不足时保持取消并说明各物品的缺少数量。
_避免使用_：撤销删除

**标准金额**：
创建预约时由预约项目组合自动汇总的金额快照，用于表示本次服务内容当时的常规价格；待执行预约改变项目组合时随之重新生成。
_避免使用_：原项目金额、原价

**成交金额**：
服务完成时确认的实际成交金额；待执行阶段不填写，完成确认时必须填写并默认带出标准金额。优惠或加价仅由成交金额与标准金额的差额推导。第一版不区分应收、已收、欠款或收款方式。
_避免使用_：约定金额、折后金额、最终金额

## 顾客

**顾客**：
持续维护并由预约引用的联系人，以唯一昵称和通过校验的中国大陆 11 位手机号识别；昵称和手机号在所有未删除顾客中都不可重复，停用后仍占用，列表直接展示完整手机号。预约始终展示顾客当前资料，不保存顾客资料快照。
_避免使用_：客户、会员、预约人

**顾客服务地址**：
顾客可维护的多个上门服务地点，每项包含地址正文和地址备注，且不设置默认项；创建预约时由用户手动选择，并将所选内容复制为预约地址快照。
_避免使用_：默认地址、顾客住址

**临时服务地址**：
仅用于本次预约且不必预先存在于顾客地址列表的地点；用户可选择在录入时将其同时保存为顾客服务地址。
_避免使用_：其他地址

**停用顾客**：
已有预约记录但不再参与新预约的顾客；其资料和历史预约继续保留，可在需要时重新启用。只有从未关联预约的顾客才能彻底删除。
_避免使用_：删除顾客、黑名单顾客

## 业务模块

**已解锁模块**：
通过本地模块授权码启用并持续可访问的业务模块；第一版只允许增加已解锁模块，不允许主动移除。
_避免使用_：已购买模块、可删除模块

**全局工作台**：
应用级模块门厅，只展示已解锁业务模块并进入对应模块；模块管理、系统级数据保护和其他跨模块能力归属“我的”。全局底部导航只包含工作台与我的，不承载任一业务模块专属的日程、报表或数据页。
_避免使用_：美容首页、模块首页

**模块工作台**：
某个业务模块内部的首页及其专属导航空间；模块可以拥有自己的首页、日程、报表和数据等页签，切换模块内页签不改变全局导航语义。
_避免使用_：全局工作台、全局日程

## 数据保护

**本地备份文件**：
声明了备份范围并包含相应本地数据和顾客资料的未加密 JSON 快照；范围可以是完整系统或一个以上业务模块。用户导出或恢复前必须获知其包含敏感资料并自行妥善保管。
_避免使用_：加密备份、云端备份、自动同步

**完整系统备份**：
包含个人设置、模块授权状态以及所有业务模块数据的本地备份；它用于整机迁移或完整灾难恢复。
_避免使用_：美容备份、部分备份

**模块备份**：
只包含用户所选一个或多个业务模块数据的本地备份，不包含个人设置和模块授权状态；系统级入口可以选择模块，模块内数据页默认选择当前模块。
_避免使用_：完整系统备份、当前页面备份

**最近导出时间**：
最近一次成功生成备份并完成文件分享流程的时间，也是七天备份提醒的计算基准；它不保证聊天中的文件仍然存在。
_避免使用_：最近成功备份时间、云端备份时间

**七天备份提醒**：
存在业务数据且距首次业务数据或最近导出已满七天时显示的保护性提醒；第一版周期固定且不能永久关闭，用户只能立即导出或稍后处理。选择稍后后同一自然日不再提醒，第二天打开且仍未导出时继续提醒。
_避免使用_：可关闭提醒、自定义备份周期

**恢复覆盖确认**：
已有业务数据时恢复备份前的明确风险确认；用户可以先导出当前数据，也可以确认理解后直接整体覆盖，系统不强制先导出。
_避免使用_：合并恢复、强制备份

**完整恢复**：
从有效备份整体恢复业务数据、已解锁模块和个人设置；尚未解锁模块的新设备也可直接从模块激活页发起。
_避免使用_：仅恢复业务数据、合并导入

**选择模块恢复**：
从有效模块备份替换文件声明范围内的模块数据；未选择模块的数据、个人设置和模块授权状态必须保持不变。它不是把备份记录与现有记录逐条合并。
_避免使用_：完整恢复、合并恢复

**兼容恢复**：
当前版本可恢复本版本和受支持的旧版备份，并在需要时迁移；由更新应用版本生成的备份必须先升级应用，未知或校验失败的文件不得改变当前数据。
_避免使用_：降级恢复、部分恢复

**手动历史清理**：
本地容量紧张时由用户主动筛选和删除历史业务记录；系统只能提前提醒并引导导出备份，不得自动或静默删除任何业务数据。
_避免使用_：自动清理、自动归档

**删除已完成预约**：
从预约历史和经营统计中移除一条已完成预约，但不补回已经实际消耗的库存；其库存变动记录继续保留并标记来源预约已删除。误完成必须使用撤销完成，而不是删除。
_避免使用_：撤销完成、退回库存

**删除待执行预约**：
彻底移除一条误建的待执行预约并立即释放其库存占用；真实发生的顾客取消应使用取消预约，以保留业务事实。
_避免使用_：取消预约

**删除已取消预约**：
彻底移除一条已取消预约及其取消说明，不改变库存，也不再参与任何历史统计。
_避免使用_：恢复取消
